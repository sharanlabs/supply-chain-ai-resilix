# Component G gate evidence — 3-layer a11y CI + G-3/G-4 + SCA

**Increment:** component G (ALIGNMENT-AUDIT: deploy/infra a11y CI + the deferred E target-size/contrast). **Date:** 2026-06-18. **Maker:** sharank98 (Opus 4.8), autopilot, push HELD. **Commit:** `c676f46` (+ pending closure commit).

## What G is
The accessibility CI for the V2 ActionOps surface, made of the three layers the audit names (axe catches ~57% by volume, so it is necessary-not-sufficient): **L1** `@axe-core/playwright` (WCAG 2.2 A/AA), **L2** keyboard-only Playwright specs (ARIA APG tablist + SC 2.4.11), **L3** a manual screen-reader pass (the human step — only a person confirms a live region actually announces). Plus the two deferred E criteria: **G-3** SC 2.5.8 target-size (24px) and **G-4** SC 1.4.11 non-text contrast. Plus the G dependency/SCA item (`npm audit`). The `verify:live` + `coverage` half of audit-component-G was already shipped (P3.2/F).

## Key correction (verify-before-build)
The HANDOFF "first design question" was **stale**: it claimed the V2 view is jsdom-fixture-only with no browser route. Reading the code showed Phase 8 (`c1c7ea5`) already wired `ActionOpsPacketView` into the live `/` route as the **default "packet" tab** (`launchops-dashboard.tsx:87,268`) with the rich `makeDemoPacket()`. So G points axe at the **real shipped surface** — no synthetic demo route (anti-scope). HANDOFF corrected + lesson logged.

## What was built / changed
- `evals/e2e/a11y.spec.ts` (NEW) — 10 specs. L1: axe WCAG 2.2 A/AA over all 4 tabs + the post-approval state (assert `violations==[]` AND triage axe `incomplete`). L2: APG roving-tabindex + arrow nav + Tab-into-panel, `:focus-visible` ring, SC 2.4.11 focus-not-obscured (real focus-scroll past the sticky masthead). G-3: every author-styled interactive target ≥24px (native checkbox excluded — UA-control exception). G-4: bar boundary ≥3:1 from ground-truth sRGB via in-browser canvas.
- `components/action-packet-view.tsx` — 4 a11y fixes (below).
- `app/globals.css` — `:focus-visible` extended to form controls; `--color-runway-edge` token + 1px `.runway-fill` border (SC 1.4.11).
- `docs/claude/A11Y-MANUAL-SR-PASS.md` (NEW) — the L3 manual checklist (OWNER action, not faked).
- `package.json`/`package-lock.json` — `@axe-core/playwright@4.11.3` (**MPL-2.0** — corrected from the stale HANDOFF note that said MIT; MPL-2.0 is OSI-approved/free and, as a dev-only test dependency we never modify or ship, imposes no copyleft on the app — stack-rule OK) dev dep + `npm audit fix` (non-force).

## Real AA failures the automated judge caught (maker ≠ judge)
Phase 8 was merged "WCAG 2.2 AA — badge ink tokens measured ≥4.5:1". The axe CI found 3 real failures that self-claim missed, all packet-tab, all settled-DOM (not animation artifacts):
1. `opacity-80` on the VERIFIER mono text dragged accent-strong to **3.95:1** → removed (full token ≥4.5, proven by the un-dimmed parent line passing).
2. status pill used `text-caution` not the AA-tuned `text-caution-ink` token that already existed → **3.57:1** → switched to `text-caution-ink`.
3. presentational `<dl>` (text+span children, no dt/dd) tripped `definition-list` → `<div>`.

## Animation-settle (the false-green trap)
axe over an unsettled DOM reported ~28 PHANTOM contrast nodes (mid-fade opacity blends `--color-ink-faint` to `#b5b9b6`/1.92 vs its true ~4.5+). All vanish after `await document.getAnimations().finished`. Playwright `reducedMotion:'reduce'` is NOT honored by this app's matchMedia (verified false) — every scan/measurement settles explicitly instead.

## G-3 / G-4 measurements (ground truth)
- **G-3:** evidence links were 16px → padded to `min-h-6` (24px). Full target sweep: tabs 48px, buttons 40–48px, links 24px; only sub-24 target is the native checkbox 13×13 (UA-control exception, excluded).
- **G-4:** fill-vs-track contrast low **1.02** (near-invisible) / med 3.31 / high 3.45 / crit 5.21 → hue-independent 1px `--color-runway-edge` at **4.37:1** vs the track carries every severity; numeric label beside each bar separately satisfies SC 1.4.1.

## Verify (independent) — GREEN
- `npm run verify` → **297 passed / 10 skipped**, exit 0 (typecheck + lint + test + build + secrets clean). The 10 skips are env-gated DB/live-network tests, not a11y.
- `npm run test:e2e` → **11 passed** (the V1 `launchops.spec` flow + the 10 a11y specs). a11y specs run in CI today (verify.yml installs chromium + runs `test:e2e`).
- `npm run coverage` → exit 0. stmts 82.18 / branches 72.07 / funcs 80.37 / lines 82.43 — floor holds (e2e specs don't move unit coverage; no covered code removed).
- `npm audit --audit-level=high` → **0 high** (was 4: fast-uri/next/vite/ws, cleared non-breaking; next 16.2.4→16.2.9, lockfile-only).

## SCA deferrals (tracked, not silent)
`--force` fixes deferred with rationale: the `@ai-sdk` chain advisory is off the active path (live-AI OFF → fix in D's Gemini-SDK upgrade), and the esbuild "fix" is a drizzle-kit 0.31→0.18 **downgrade** (a regression — must not apply). Remaining 4 low / 6 moderate are below the CI gate threshold.

## Gate panel
1. **grill / non-vacuity (inline, same-family — the acceptance-gate subagent stream-idle-timed-out, infra not logic; not retried blind per the P8 lesson):** each spec verified to test what it claims — settle() prevents mid-animation false-greens; 2.4.11 forces a real scroll (`scrollY>0`) then asserts rect.top ≥ masthead bottom; G-3 checks width AND height and now sweeps ALL author-styled targets (no silent cap); the axe `incomplete` allow-list (contrast/target-size) cannot hide a real gap because contrast resolves to `violations` and target-size is independently measured; the approved-state scan reaches a genuinely different DOM. PASS.
2. **codex cross-model (mandatory, independent):** **REVISE → all 6 findings resolved** (verdict + per-finding resolution below). The cross-model gate VINDICATED itself: it found 6 real issues the same-family inline read rated PASS, while correctly refuting the false alarms.
3. **verify-correctness:** the three fixes achieve AA (proven by the re-scan passing incl. the approved state); no regression (jsdom 297 + V1 e2e green). PASS.
4. **enterprise + taste:** layered structure, WHY-comments, documented V1-panel exclusion + owner-gated L3 + SCA rationale; fixes minimal + token-consistent (reused `caution-ink`, one new edge token), iter-3 preserved. PASS.
5. **anti-slop:** comments explain WHY (settle/oklch/canvas) and state measured ratios; no hollow comprehensiveness. PASS.

## Scope / deferrals (deliberate)
- L3 manual SR pass = OWNER action (`A11Y-MANUAL-SR-PASS.md`), not automated.
- G-10 approval-UX-vs-automation-bias = owner-gated (taste/product), out of scope.
- V1 live panel = out of the a11y scan scope (mounts only after a live run).
- Push HELD (owner-gated). Live-AI OFF.

## Codex cross-model verdict
**Round 1: VERDICT: REVISE** (reviewed `c676f46`; 6 findings — 5 Med, 1 Low). Codex REFUTED the false alarms (settle() is called; approved-state reaches a changed DOM; target-size checks W+H; caution-ink + un-dimmed verifier clear AA; runway-edge clears 3:1; V1 exclusion disclosed). All 6 findings weighed (primary-model-final) and held — none refutable. Resolutions:

1. **[Med] incomplete allow-listed by rule id** (`a11y.spec.ts`). FIXED: factored `assertAxeClean(results, label)` that asserts `violations==[]` AND that the only `incomplete` rule is `color-contrast` (any other undecided rule fails loud). The residual color-contrast incompletes are text over the rail CSS gradient (axe cannot composite a gradient); a dedicated spec ground-truth-measures **each axe-incomplete node** against the gradient's darkest effective stop (accent-soft/60 over surface) at the correct WCAG threshold. Also fixed the real issue this surfaced: the `opacity-70` tab-index numerals were SUB-AA (masked as incomplete) → removed the opacity (`tab-nav.tsx`).
2. **[Med] approved-state scan skipped the incomplete triage.** FIXED: it now calls the same `assertAxeClean` helper.
3. **[Med] SC 2.4.11 only proved scrollY>0 (vacuous).** FIXED: now scrolls the link fully out of view, ASSERTS it is obscured (above the masthead) pre-focus, then asserts the focus drove a real corrective scroll (scrollY changed) AND the link landed below the masthead.
4. **[Med] G-4 checked only `.runway-fill.first()`.** FIXED: now iterates EVERY `.runway-fill` on every bar-bearing tab (exposure/simulation/packet — solid + gradient fills), comparing each edge against ITS OWN track background, with a ≥5-bar sanity floor.
5. **[Med] invalid package graph** (`vite@8.0.16` wants esbuild ^0.27/^0.28; npm deduped to drizzle-kit's esbuild@0.25.12 → `npm ls` ELSPROBLEMS). **Verified + DEFERRED with rationale (primary-model-final):** it is **pre-existing** (vite 8.0.10 had the same invalid dedup *before* component G; the SCA only bumped the patch 8.0.10→8.0.16), **`npm ci` succeeds** (the CI gate is unaffected — ELSPROBLEMS is an `npm ls` validity warning, not an install failure), and `npm audit --audit-level=high` is 0. A scoped `overrides` (vite>esbuild) does NOT apply without a full `package-lock.json` regeneration, which would re-resolve every caret range and risk re-introducing the cleared highs — not worth it for a cosmetic, CI-passing, pre-existing warning. **Tracked for a dedicated dependency-hygiene increment** (align drizzle-kit's esbuild or deliberately re-nest vite's, with its own gate). A no-op override was attempted and reverted (a dead override is itself slop).
6. **[Low] license note said MIT; actual MPL-2.0.** FIXED: corrected here and in HANDOFF; MPL-2.0 is OSI-approved/free and, as a dev-only test tool we never modify/ship, imposes no copyleft on the app — stack-rule OK.

**Re-verify after fixes — GREEN:** `npm run verify` 297 passed/10 skipped; `npm run test:e2e` **12 passed** (V1 + 11 a11y); `npm run coverage` 82.18/72.07/80.37/82.43 (floor holds); `npm audit --audit-level=high` 0.

**Round 2 (closure): VERDICT: REVISE** (reviewed `c676f46..f489843`). Codex confirmed findings 2,3,4,5,6 genuinely resolved/soundly-deferred, but found finding 1 only PARTIALLY resolved + a doc-honesty miss — 3 more (2 Med + 1 Low), all held:
1. **[Med] `assertAxeClean` permitted color-contrast incomplete in every scan, but the ground-truth measurement only ran on the default packet state** → Events/Exposure/Simulation/approved incompletes passed UNMEASURED. FIXED: folded the ground-truth measurement INTO `assertAxeClean(page, results, label)`, so EVERY scan measures its own incomplete nodes (the standalone gradient test was removed as redundant).
2. **[Med] the measurement ASSUMED every incomplete node sat on the rail gradient** (false-green for an unrelated incomplete). FIXED: each incomplete node is now asserted to be `el.closest(".bg-gradient-to-b") !== null` (inside the known rail gradient) before measuring; anything else fails loud.
3. **[Low] the gate doc claimed the HANDOFF MIT note was corrected, but it still said MIT** (an honesty gap). FIXED: corrected the HANDOFF stale phrase to MPL-2.0.

**Re-verify after round-2 fixes — GREEN:** verify 297 passed/10 skipped; test:e2e **11 passed** (V1 + 10 a11y); coverage 82.18/72.07/80.37/82.43; audit-high 0.

**Round 3 (closure-confirm):** _running on `f489843..HEAD` — appended below._
