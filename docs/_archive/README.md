# Archived — predecessor (LaunchOps / RESILIX-v1) build docs

These documents describe the **predecessor** system (LaunchOps / RESILIX-v1), not the
current ActionOps product. They are retained for history and are **not** current
documentation. For the current system see the repo root `README.md`, `PLAN.md`, and
`docs/architecture.md`. Each file carries its own stale banner.
