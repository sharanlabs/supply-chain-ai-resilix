# Gate evidence — D.3-D.8 (ActionOps agent core) — BATCHED 360-degree + Codex review — 2026-06-18

The owner directed Codex/heavy review to BATCH at large checkpoints (not per-increment) for
token/quality balance. This is the batched gate over **D.3 (Simulator) · D.4 (citation
gatekeeper) · D.5 (Sentinel+firewall+model-fix) · D.6 (Strategist) · D.7 (Dispatcher) · D.8
(cost ledger)**. All key-OFF / deterministic; the live LLM paths are BUILT but UNWIRED (they
fire at D.9). Each increment had: maker -> independent `npm run verify` -> orchestrator review
-> commit `wip`. D.1-D.2 were already Codex-APPROVED separately.

## The 360-degree review team (parallel, multi-lens)
- **security-specialist** (injection/exfil threat model): the core is strong (lethal-trifecta
  laundering cut HOLDS, fail-closed firewalls, prototype-pollution-safe resolveSourcePath,
  pricing fail-loud). Findings: [HIGH] scheme-less/bare-domain URL bypass (exfil); [MED->HIGH]
  cumulative budget unenforced (default spentUsd:0); [LOW->MED] non-ASCII-digit numeral bypass.
- **evals-specialist** (f(x)===f(x) + grader teeth): the f(x)===f(x) defeat is REAL (re-derived
  the D.2/D.3/D.8 hand-pins independently; the golden-tasks independent anchors are the strongest
  part). Findings: [HIGH] `untrustedRawStrings` always `[]` -> the injection text-leak grader is
  blind at key-ON; [MED] cumulative budget; [LOW] runout fixture can't discriminate floor.
- **acceptance-gate** (enterprise/taste + anti-slop): **SHIP** — the house LLM pattern holds
  with no drift D.5->D.8; fail-closed posture real; cost ledger enterprise-grade; zero slop
  tells (precise-codepoint grep); honesty disclosed (demo-fallback, cost cumulative-gap).
- **guidelines-monitor** (live external canon): **FOLLOWS/EXCEEDS the 2026 canon** — OWASP LLM
  Top-10 2025 (LLM01/02/05/06 each mapped), Meta Rule-of-Two (the Dispatcher cuts to two +
  human gate), Willison lethal-trifecta, the f(x)!=f(x) anchors, the budget hard-stop, versioned
  pricing, ListModels-preflight all vendor-endorsed. Finding: [HIGH] `gemini-2.5-flash` is
  OFFICIALLY DEPRECATED (Google deprecations page 2026-06-15; shutdown **2026-10-16**) + the
  purpose-built preflight is UNWIRED; pricing pin ($0.30/$2.50) confirmed correct as-of now.
- **Codex cross-model** (existing ChatGPT account, post-reset): **REVISE** — [HIGH] scheme-less/
  bare/encoded URL bypass (convergent); [HIGH] same-value wrong-sourcePath not enforced
  (Codex-unique); [MED] spelled-out-quantity bypass; [MED] D.8 budget not universal (legacy path
  bypasses the guard); [MED] cumulative budget default unsafe.

## Reconciliation (primary-model-final) + fixes APPLIED (`476c81c`, verify GREEN 416/10)
The deterministic-layer findings were fixed now (each mutation-verified to bite; Hormuz 124/124):
- **[HIGH] Robust shared link detector** (`lib/pipeline/url-detect.ts`) — scheme/bare-domain/
  protocol-relative/markdown/HTML/entity-encoded, wired at the Dispatcher firewall + Sentinel
  summary + gradeEvidence. Exfil bypass closed.
- **[HIGH] Exposure-claim supplier binding** (`citation-check.ts`) — an `exposureResults[n].
  exposureScore` claim must resolve sibling `[n].supplierId === msg.supplierId`. Same-value
  wrong-sourcePath closed (the Success_Criteria:34 negative now enforced).
- **[MED] Word/non-ASCII-digit numerals** flagged in the unsourced-numeral gate (FP-guarded).
- **[LOW] Floor-discriminating runout fixture** (1030/40=25.75 -> 25).

## BINDING D.9 PRE-CONDITIONS (live-path findings — inert key-OFF, MUST close before key-ON)
The reviewers framed these as "BLOCK key-ON until" — they only bite when the live LLM paths are
wired (D.9). Tracked here + in the HANDOFF + `tasks/todo.md`, NOT silently dropped:
1. **Feed `untrustedRawStrings`** from the run's raw pre-sanitization signal text into the
   `runGraders` ground truth, so `gradeInjectionQuarantine`'s text-leak half bites when the LLM
   Sentinel ingests raw GDELT article text. + a live-packet test with a run-derived denylist.
2. **Cumulative cross-agent budget**: thread a single running `spentUsd` (sum of each agentRun's
   real `costUsd`) into each live call; make a live call FAIL CLOSED if no cumulative budget
   context is supplied (so D.9 wiring cannot inherit the per-call-only cap). Route/guard the
   legacy `runLaunchOpsAgents` live path (currently bypasses `assertWithinBudget`) — or disable it
   when `ENABLE_LIVE_AI=true`.
3. **Wire `assertConfiguredModelAvailable`** into the live-AI startup; `gemini-2.5-flash` shuts
   down **2026-10-16** — at enablement, verify the successor model id via ListModels ON THIS KEY
   (do NOT hard-code a successor). Record the migration date.
4. **UI display-layer escaping** (OWASP LLM05 layer-6) — verify the redesigned UI renders packet
   prose as text, not trusted markup. -> the UI increment.
5. **LLM-judge TPR/TNR calibration** (G-5) before the judge gates anything. -> D.9.

## Status — D.3-D.8 GATED key-OFF (2026-06-18)
The key-OFF spine SHIPS: `npm run verify` GREEN (416/10) + `test:e2e` GREEN (a11y) + the 4-lens
360-degree review (acceptance-gate SHIP, guidelines FOLLOWS/EXCEEDS, security+evals: sound core)
+ Codex REVISE -> deterministic findings FIXED + verified, live-path findings tracked as binding
D.9 pre-conditions. Committed key-OFF (`a8ce3ef`..`476c81c`). A Codex closure round can confirm
the fixes at the next checkpoint (the live-path items are intentionally D.9-deferred, so they
would re-surface as "open by design"). **NEXT: the UI redesign workstream, then D.9 (owner-gated).**
