# API keys and secrets
.env
.env*.local
*.key
secrets/

# Dependencies and build output
node_modules/
.next/
next-env.d.ts
out/
coverage/
playwright-report/
test-results/
dist/
*.tsbuildinfo

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Temporary files
*.tmp
*.bak

# Local Claude Code working dir (personal settings + transient automation reports;
# no shared project config lives here)
.claude/

# Quarantined legacy n8n pipeline exports (reference only; project reoriented to
# ActionOps). Kept local, isolated from the active codebase per the quarantine
# invariant. The ACTIVE workflow is tracked at workflows/launchops_approval_workflow.json.
resilix_pipeline_v2*.json
