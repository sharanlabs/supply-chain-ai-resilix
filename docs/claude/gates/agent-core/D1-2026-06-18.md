# Gate evidence — D.1 (V1→V2 decision-packet cutover) — 2026-06-18

**Increment:** D.1, the keystone of Component D (the ActionOps agent core). The live
pipeline + the `/` dashboard render path + the API now emit/consume the **V2**
ActionOps packet; the LaunchOps **V1** producer is removed from the pipeline (the V1
schema STAYS for back-compat). Key-OFF (deterministic placeholders; no live AI).

## What changed (the coupled flip)
- **(2b)** `lib/pipeline/run-exception.ts`: `runExceptionPipeline` = `buildDecisionPacket`
  (pure V2 producer) + `saveDecisionPacket` + the existing idempotency mutex. Deleted
  the V1 body `executeExceptionPipeline`; dropped the LaunchOps engine imports. Returns
  the `DecisionPacket` UNION (fresh run = V2; the idempotency double-check can return a
  stored packet of either version).
- **(3)** `app/page.tsx`: async server component → `buildDecisionPacket({useLiveSignals:false})`
  (pure, NON-persisting, cached signals → no per-load save, no request-time network) →
  passes the real V2 to `components/launchops-dashboard.tsx` (new `packet` prop;
  `makeDemoPacket()` is the defensive fallback). Pipeline-failure path now logs
  server-side before falling back (was a silent swallow — fixed per the acceptance-gate).
- **Option A** — the V1 LaunchOps "Run live pipeline" panel + its e2e
  (`evals/e2e/launchops.spec.ts`) were RETIRED. Rationale: `ActionOpsPacketView` approves
  client-side/optimistic, while a run-produced packet IS persisted, so a "Run→Approve"
  UI would show APPROVED while the store stayed PENDING (a DB/UI provenance divergence).
  Run+approve intent is now covered by the default surface + the a11y approve test + the
  API-level evals.
- **(4)** Tests migrated to V2: new `evals/fixtures/decision-packet-v1.ts` (`makeV1Packet()`,
  the V1 oracle assembled via the retained LaunchOps engine) for the back-compat union +
  legacy-read tests; `pipeline.test` → V2 + persistence; `agent-mode` legacy-shim →
  `makeV1Packet()`; `decision-packet-version` → `makeV1Packet()`; `concurrency-integrity`
  spy → `buildDecisionPacket` (the new expensive boundary, direct import); `api-hardening`
  / `security-fail-closed` scenario id `SCN-LAUNCH-001`→`SCN-HORMUZ`. `n8n-callback` +
  the gated `db-*` tests are version-agnostic (unchanged). `evals/actionops-pipeline.test.ts`
  (added in sub-step 2a) asserts `runGraders(packet, gt).blocked === false` over the live
  V2 output.
- **Bundler fix surfaced by the cutover:** `lib/ingest/seed-suppliers.ts` `SEED_CSV_PATH`
  `fileURLToPath(new URL(..., import.meta.url))` → `path.join(process.cwd(), ...)`. The
  page cutover put this module in the `/` build graph; Turbopack rewrites `import.meta.url`
  to the chunk location and the bundled `URL` fails `fileURLToPath`'s cross-realm
  `instanceof` check → `next build` "collect page data for /" threw `ERR_INVALID_ARG_TYPE`.
- `app/api/run-exception/route.ts`: dropped `scenarioId`'s `.default("SCN-LAUNCH-001")`
  (retired id) so an omitted scenario flows to `DEFAULT_SCENARIO_ID` (SCN-HORMUZ).

## Gate results
- **verify** — GREEN. `npm run verify` = typecheck + lint (eslint `--max-warnings=0`) +
  **299 passed / 10 skipped** (the 10 skips = env-gated DB/live-network) + `next build`
  (`/` prerenders static) + secret-scan (no patterns). The F organ-8 contract
  (`evals/golden-tasks.test.ts` + `runGraders`) rides inside `npm test`.
- **test:e2e** — GREEN. `npm run test:e2e` = **10/10** a11y on the REAL pipeline packet on
  `/`: axe WCAG 2.2 AA over all 4 tabs + the approved state; keyboard APG (roving tabindex
  / arrow nav / Tab-into-panel); SC 2.4.11 focus-not-obscured; SC 2.5.8 target-size; SC
  1.4.11 non-text contrast. The cutover preserved the rich structure (4 tabs +
  approved-state) the demo rendered — the HANDOFF's explicit requirement.
- **acceptance-gate** (independent read-only subagent) — gates **4 (enterprise/taste) PASS**
  and **5 (anti-slop) PASS**: cutover well-structured (assemble/persist split, union
  return sound, `makeV1Packet` a faithful oracle genuinely consumed, LaunchOps retention
  justified, the `buildDecisionPacket` spy a real single-execution proof); no slop tells;
  the DB/UI divergence is NOT a honesty fail (approve copy is documented-optimistic, never
  claims durable dispatch). One finding — silent `catch` in `app/page.tsx` — **FIXED**
  (server-side error log added). Gates 1/3 it flagged are evidence-handoff items (this
  file + the transcript). Gate 2 (Codex) below.
- **Codex cross-model** — **REVISE(8) → 5 fixed in D.1, 3 tracked; closure round pending.**
  (First run hit the shared-seat usage limit mid-review with no verdict; the re-run after
  the ~16:38 reset completed.) Codex validated the core flip ("the V1 union return and V1
  oracle look type-honest; the migrated concurrency spy has a useful distinct-key control")
  — the 8 findings are edge-honesty/polish, weighed by primary-model-final (advisor-reconciled):
  - **#2 High — FIXED.** Approve copy claimed "is logged" / "writes an audit entry" while
    the non-persisted `/` packet only mutates client state → softened to "records a local
    review decision in the audit trail" (`components/action-packet-view.tsx`).
  - **#1 High — FIXED (copy) + tracked (semantics → D.4).** "Corroborated across N sources"
    counted the generic feed as threat corroboration → "Across N recorded sources"
    (descriptive); the computed `evidenceAllowlistPassed` line stays. Relevance-filtering is
    the D.4 Verifier's job (tracked in `tasks/todo.md`).
  - **#3 Med — FIXED.** `/` was build-time static → `export const dynamic = "force-dynamic"`
    (`app/page.tsx`); the build now shows `ƒ /` (was `○`). Fresh per request, no network.
  - **#5 Med — FIXED.** Unknown `scenarioId` → 500 → now `400 INVALID_SCENARIO_ID` via the
    non-throwing `hasActionOpsScenario` route check (+ an api-hardening test).
  - **#7 Low — FIXED.** Added an a11y-e2e provenance guard asserting `/` renders the real
    pipeline packet (id `DP-<uuid>`), not the demo fallback (`DP-DEMO-HORMUZ`).
  - **#4 Med / #6 Med — TRACKED.** #4 = the P2.1-documented single-instance idempotency
    limit (preserved verbatim, not introduced). #6 = `requestedMode=LIVE_AI` honoring →
    correct in D.1 key-OFF, a D.5 verification point. Both in `tasks/todo.md`.
  - **#8 Low — REFUTED (no action).** cwd seed path is correct-by-construction for Next's
    run model; `readFileSync` already throws `ENOENT` with the path.
  - Re-`verify` GREEN (300 passed/10 skipped — +1 scenario-400 test) + re-`test:e2e`
    GREEN (11 — +1 provenance guard).
  - **Closure round (REVISE×1):** one item not fully closed — the rendered `threatCard.summary`
    still claimed "a partial closure window is corroborated across independent outlets" (the
    deterministic Sentinel copies the scenario summary verbatim; relevance-filtered
    corroboration is D.4's). Neutralized in `lib/data/actionops-scenarios.ts` +
    `lib/data/demo-packet.ts` ("...is in effect."; a fallback signal "corroborate"→"report")
    + smoothed the approval string's literal `--`. Re-`verify` GREEN (300/10) + re-`test:e2e`
    GREEN (11).
  - **Closure round 2: `VERDICT: APPROVED`.** Codex confirmed both old phrases gone repo-wide,
    the evidence line reads "Across N recorded sources", D.4 deferral tracked, and explicitly
    accepted the `verifier.ts` placeholder boolean as a tracked-to-D.4 deferral ("not a D.1
    blocker"). The mandatory cross-model gate is fully discharged.

## Status — D.1 DONE (gated, 2026-06-18)
Gate fully discharged: maker → `npm run verify` GREEN (300/10) → `npm run test:e2e` GREEN (11)
→ acceptance-gate gates 4+5 PASS → **Codex REVISE(8)→5-fixed/3-tracked → closure REVISE(1)→fixed
→ closure-2 APPROVED.** Committed key-OFF (`d5e18a8` flip → `44f7d0c` Codex fixes → `03375f2`
closure fix → finalize). Push HELD. 3 tracked items in `tasks/todo.md` (D.4 corroboration
semantics · D.5 mode-honoring · post-MVP cross-instance idempotency). **NEXT: D.2 (Atlas
deterministic exposure scoring).**
