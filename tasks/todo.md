# todo — project tasks

## Idea (2026-06-11 21:20)
> RESILIX ActionOps — crisis-to-action war room: live disruption signal + supplier CSV → evidence-cited, human-approved action packet in under 5 minutes

## ⚠️ SUPERSEDING POINTER (2026-06-25) — NEW INITIATIVE: agentic rework

A new owner-locked initiative is underway, **ADDITIVE** on the shipped ActionOps product below: turn RESILIX into a **governed multi-agent system** (tool-using Investigator + cross-family Skeptic + governed action execution). **Phase 0 (design-grill) DISCHARGED 2026-06-25 — Codex APPROVED in 3 rounds, zero code.** Owner decisions: **REORDER** (value-first/loop-last-gated) + **`/autopilot`**. **NEXT: `/autopilot` → Phase 1 = the P1 domain wins.** Full resume context: `docs/claude/HANDOFF.md` (top "CURRENT RESUME (2026-06-25)" block) + `~/.claude/plans/read-last-handoff-and-keen-globe.md` + gate artifacts `docs/claude/gates/agentic-rework/`. The ActionOps status below remains the working baseline the rework builds on.

**Owner steer (2026-06-25, mid-Phase-1):** align the tech stack to the project GOAL — free-first is the default, NOT a ceiling; paid/enterprise/low-cost-to-showcase tools are on the table where they serve the goal. Binds at P4/P5/P6 + model tier, NOT Phase 1 (pure deterministic domain, zero new deps). Memory `resilix-tech-stack-alignment`.

## ▶ AGENTIC REWORK — Phase 1 (P1 domain wins) — IN PROGRESS (autopilot, 2026-06-25)

**Stage: execution, Phase 1 of 7.** Pure deterministic-waterfall domain-credibility wins (NO tool layer / NO LLM loop — that is the P3 cluster, built last). `ENABLE_AGENT_LOOP` not introduced here. Flag-off waterfall stays green throughout. Each increment: maker → `npm run verify` green → (acceptance-gate / 360° as warranted). **Codex cross-model BATCHED once at the Phase-1 checkpoint** (owner efficiency doctrine, not per-increment). Push HELD (owner-gated).

Sequenced per advisor (low-risk green progress first, coupled spine in the middle):
- [x] **P1.1 — Threat enum +3** (`MATERIAL_SHORTAGE_ALLOCATION`/`EXPORT_CONTROL`/`QUALITY_RECALL`) → `schemas.ts` + Sentinel recognition (auto-propagates via `resolveEventType` + the live-prompt `.options` list). +5 firewall recognition tests. ✅ DONE 2026-06-25, `npm run verify` GREEN (520/21).
- [x] **P1.6 — Ops + Finance playbooks** (only Procurement shipped) → `strategist.ts deterministicPlaybooks` now emits Procurement + Operations + Finance, numeral-free, grounded in the same top-3 exposure ids. 4 strategist assertions updated. ✅ DONE 2026-06-25, `npm run verify` GREEN.
- [x] **P1.2 + P1.3 — quantitative spine** (co-built; share `simulation-math`/`simulator`/the simulator test, linked through TTS). ✅ DONE 2026-06-25, `npm run verify` GREEN (521/21, exit 0).
  - **Single-source/TTR:** `ExposureResultSchema` += `singleSource`/`recoveryDays` (additive-optional). Atlas adds a +12 single-source score penalty + TTR (= lead time, +14 when single-source) + a rationale clause (tier-token-first preserved). **ADVISOR #1 fixed:** `linkBackupSuppliers` seed-only overlay (CRITICAL/HIGH = single-source; MEDIUM/LOW = backup → LOW/MEDIUM same-sector alternate in another country) → Gulf-9 split **5 single-source / 4 covered** (penalty discriminates, not a constant). P2.5 upload path stays `null`.
  - **TTS/margin/revenue-from-runout:** `SimulationSchema` += `survivalDays` (TTS) + per-horizon `marginAtRiskUsd` (additive-optional). `recomputeSimulation` now anchors the revenue clock at runout: `exposedDays(H)=max(0, min(H,duration)−survivalDays)`. Hormuz headline goes $2.7M→**$450k** (honest: covered for the 25-day runout, loss only in the 5-day gap to disruption-end). `marginPct` on sim params; margin rounded to whole USD (float-safe).
  - **Blast radius (ADVISOR #3):** golden sim auto-tracks (derived via `recomputeSimulation`); grader auto-tracks + extended with margin/TTS teeth; 2 independent hand-derived oracles updated (`actionops-simulator.test.ts` + `golden-tasks.test.ts`); homepage renders a FROZEN pre-P1 fixture so additive-optional fields keep it parsing — **the wins surface on `/` only after a live re-capture of `SCN-HORMUZ.json` (owner-gated billable run); deferred**. demo-packet regen + rich UI surfacing of margin/TTS/coverage deferred to end-of-phase / P6 war-room UI.
- [x] **P1.4 — Scored recovery options into V2** ✅ DONE 2026-06-25. New `lib/agents/actionops/recovery.ts` deterministic producer (EXPEDITE/REALLOCATE/SUBSTITUTE/SUPPLIER_ESCALATION, scored from exposures+sim; reversibility = the governance dial). `recoveryOptions` additive-optional on `DecisionPacketV2Schema`; wired through `ActionOpsResult`/`index.ts`/`build-packet`. **ADVISOR #2 done:** withheld (empty) in the index.ts NO_ACTION branch AND added to the `DecisionPacketSchema` superRefine (NO_ACTION → recoveryOptions empty, structurally enforced + tested). Hand-pinned Hormuz anchor (67/55/48/30). Authoritative-binding holds.
- [x] **P1.5 — Supplier-email rewrite + score-leak fix** ✅ DONE 2026-06-25. `dispatcher.ts` deterministicDrafts → numeral-free impact-assessment REQUEST (asks supplier for ship dates/on-hand/recovery/force-majeure/sub-tier), claims=[]. **Structural leak fix:** exposureScore REMOVED from the live whitelist (model never sees the internal score) + prompt rewritten + firewall REJECTS any exposureScore citation (fail-closed backstop). **ADVISOR #5 verified:** empty claims[] passes firewall/citation/injection-grader. Dispatcher + gatekeeper tests re-anchored on the window claim; new score-leak-backstop test. `demo-packet.ts` regenerated (numeral-free emails, 3 numeral-free playbooks, single-source/TTR/margin/TTS/recovery-options).
- [x] **Phase 1 gate — DISCHARGED LOCALLY (push held)** 2026-06-25/26. `npm run verify:full` GREEN (534/21 + 14 e2e, exit 0). 360°: acceptance-gate BLOCK→cleared (real demo-packet contradiction fixed + arithmetic-tested); security safe-to-proceed (score-leak verified closed; 1 LOW hardened); evals SHIP (oracles independently re-derived; margin-grader teeth). **guidelines-monitor (owner-requested SCRM domain check):** VINDICATED the core model (revenue-from-runout = exact Simchi-Levi TTS; threat types/margin/email/reversibility = genuine wins); found 2 — **both FIXED (owner: fix-both-now):** (a) single-source DECOUPLED from tier (was VIOLATED/collinear → now discriminates within tier; Gulf 2 single-source/7 covered); (c) TTR disruption-type-aware + honest label. **Homepage re-captured live** (owner-greenlit, $0.0254): clean 8/8 genuine-live, `/` now reflects the corrected model, **score leak gone from the live emails** (verified). **Codex** RAN substantively + surfaced 2 real demo-fixture findings (fixed) but hit the usage cap before a final verdict on the production logic — APPROVE stamp is a dated obligation (NOT asserted), and it's the FIRST real cross-model look at the math/security. Gate artifact `docs/claude/gates/agentic-rework/PHASE1-GATE.md`. **OWNER ACTIONS owed:** (1) Codex final stamp (after reset / on backup); (2) review + push. **TRACKED refinements:** $-at-stake in exposure rank; per-line TTS; demand-side recovery lever; scrm_kb harvest. **+ Codex FINAL STAMP RAN 2026-06-26 (cap reset): VERDICT REVISE, all 6 load-bearing targets HELD; [Med] qualitative-prose score-leak + [Low] stale comments → BOTH FIXED (`a3f1758`: a fail-closed INTERNAL_RISK_PROSE gate + 3 regression tests + comment fixes); fixtures verified risk-prose-clean. Phase-1 cross-model gate DISCHARGED.**

## ▶ REMAINING PHASES (agentic rework, reorder `P1✅→P2✅→P4✅→P5→P6→P7→P3`) — owner: complete all in-session + auto-resume on reset

- [x] **Phase 2 — Dual-LLM GDELT quarantine** ✅ DONE 2026-06-26 (`0fa2d12`). QuarantinedSignal = Omit<PublicSignal,"summary"> + quarantineSignals(); Verifier consumes the quarantined view; STATIC GUARD (`evals/quarantine.test.ts`) proves a poisoned signal/threat summary never reaches the Strategist/Dispatcher LLM prompt. verify GREEN.
- [x] **Phase 4 — cross-family Skeptic critic** ✅ DONE 2026-06-26 (`da996b1`). `skeptic.ts` (deterministic pass + Groq-Llama-4 `challengeFindingLive` + `applySkepticGate`) + shared `cross-family.ts`; wired before decideRecommendation (7-run audit); non-accept → NO_ACTION (withhold + superRefine hold); quarantine-respecting (no prose, no location.region); fail-closed; authoritative-binding (boolean gate). verify GREEN (558/22). **OPEN: the live TPR/TNR calibration is gated + UNMEASURED — run once on a Groq key.**
- [x] **Phase 5 — Governed action execution** ✅ DONE 2026-06-26 (`a73add7`). Transactional outbox; CODE-OWNED reversibility moat; NoopTransport default (no real sends); auth-gated `POST /execute` (reuses P2.7 gate); `executed_actions` table (additive `0003`); reversible auto-fire behind a default-OFF flag, outward ALWAYS human-gated. Security-reviewed safe-to-proceed (6/6 invariants); [Med] digest-sanitization fixed. verify GREEN 587/24 + gated PG-17 11. Gate doc PHASE5-GATE.md. **Real transport (Slack/SES/n8n) = a deliberate later owner step (typed seam + forward-guardrails gate it).**
- [x] **Phase 6 — War-room deliberation UI** ✅ DONE 2026-06-26 (`5185414`). Deliberation trajectory (agent chain behind a disclosure, humanized modes) + Skeptic verdict (3 honest states, gated to genuine cross-family, absent on the 6-run fixture) + recovery options on the glass (reversibility = governance dial) + P1 fields woven in (single-source data-driven, TTR, survival-vs-restore, margin); all data-driven from `agentRuns`/packet so $0 REPLAY holds; storytelling-arc EXTENDED (no redesign). +8 RTL + e2e/deliberation.spec. verify:full GREEN 596/24 + 20 e2e (WCAG 2.2 AA).
- [ ] **Phase 7 — Agentic evals + adaptive injection red-team.** Trajectory/tool-call evals; GDELT-shaped adversarial payloads (zero injection→draft/execution, zero number-laundering); NO_ACTION regression. Also P3's promotion gate. Gate: verify:full + red-team CI. Maker: evals-specialist.
- [ ] **Phase 3 — Investigator loop (LAST, optional, gated).** Wrap deterministic fns as typed closure-bound tools; `generateText({tools, stopWhen})`; bind authoritative slices from TOOL RESULTS not prose (the moat); re-call decideRecommendation in code; budget hard-stop in `prepareStep`; behind `ENABLE_AGENT_LOOP`. Promote to default-on ONLY if Phase-7 trajectory evals beat the waterfall. Gate: verify + verify:live + parity-vs-waterfall + evals-beat-waterfall. Maker: ai-engineering-specialist. **Moat invariant: [[resilix-authoritative-binding-moat]].**
- [ ] **Phase gate (per phase):** verify/verify:full GREEN → acceptance-gate → batched Codex → local commit. Push HELD (owner). Update HANDOFF + this checklist + lessons each phase boundary.

**Design calls (locked, senior-default within the plan):** TTR = scenario `supplierRecoveryDays`; recovery options in a NEW live producer; email numeral-free; margin via `marginPct`. **Authoritative-binding invariant** holds (all numbers deterministic; no LLM authority — trivially true in Phase 1, no loop).

## CURRENT STATUS (2026-06-20) — reconciled

**The full ActionOps pipeline is BUILT, GATED, and PUSHED to `origin/main`.** This block supersedes the stale per-phase checkboxes below (the gate docs under `docs/claude/gates/` are the authoritative record; the inline markers lagged behind the autopilot/Wave-2 work).

- **execution (all phases) — DONE + gated.** Phase 1 identity · Phase 2 data-at-scale (P2.1–P2.7) · Phase 3 signal layer (P3.1–P3.2) · Component D = the six-agent core D.1–D.9 (live-AI showcase) · Component F evals harness · Component G a11y CI. Each through acceptance-gate + Codex cross-model. Evidence: `docs/claude/gates/`.
- **Post-D.9 follow-ups — DONE + gated** (the row in Component D below): NO_ACTION refusal · home→frozen REPLAY · bounded +2 retry · Codex closure-2.
- **Premium 2026 UI redesign — DONE** (steel/cobalt accent, no green/pink, clean Geist sans, depth; every WCAG 2.2 AA + e2e gate held).
- **Hardening — DONE:** CSP/HSTS + standard headers (pure testable builder) · n8n callback-secret min-length · rate-limiting on the 3 mutation routes · real-HTTP upload e2e · Prettier + pre-commit · playbook-step numeral grading (already in `gradeCitationCoverage`). `npm run verify:full` GREEN (496 passed / 20 skipped + build + secrets + 15 e2e).
- **Whole-project guidelines-monitor review — DONE** (verdict: top-decile; exceeds the bar on injection security, the judge, deterministic architecture, frontend craft). Real findings applied (testable CSP, budget-claim precision, model-freshness note).
- **Full doctrine alignment pass — DONE 2026-06-22** (project-advisor + guidelines-monitor + acceptance-gate, read-only). Verdicts: need-AI gate **PASS** (AI scoped to Sentinel classification; all math/refusal deterministic; spine runs key-OFF); approach **SOUND** (2 over-builds, 1 disclosed under-build = single-instance idempotency); method **PROPORTIONATE** (cross-model gate caught real defects, not wording); ship = **SHIP-at-tier**; one best-practice **VIOLATED** = same-family LLM judge (bounded — secondary, fail-closed, off-by-default). **Three gaps closed this session:**
  1. **`verify:full` re-executed GREEN on HEAD** — **506 passed / 20 skipped + 15 e2e**, `next build` clean, secrets clean (independently confirms the prior '496' self-claim, which had grown). Gate-3 now first-hand, not reported.
  2. **Same-family judge documented + repositioned** as a SECONDARY check (deterministic graders + atomic human approval are primary) → **`docs/adr/0002-same-family-llm-judge.md`** + README Limitations + Success_Criteria + `lib/evals/judge.ts` disclosures; calibration rigor added (**Cohen's kappa + held-out dev/test split**, both unit-tested in `evals/judge-calibration.test.ts`). Cross-family switch (JUDGE_PROVIDER seam + non-Gemini SDK) + few-shot critique-then-verdict + real-draft calibration **deferred, billed-recalibration-gated** (owner chose the free close; no new dep/cost).
  3. **COOP/CORP HEAD delta (`84963e7..cac2150`) Codex cross-model leg DISCHARGED** — `codex-guarded` read-only → **APPROVE (clean)**, one tracked non-blocking coverage note. Gate-2 residual CLOSED. Evidence: `docs/claude/gates/optional-hardening-2026-06-20.md`.
- **Bucket C (expansion-path) — owner pulled forward 2026-06-22; 2 of 3 built, 1 deferred-with-reason:**
  - **C1 observability — DONE.** `lib/server/logger.ts` (pino 10, JSON-to-stdout/bundler-safe, secret-redacting, silent under test) wired at the pipeline lifecycle (`run-exception.ts`) + the signal-fetcher outage seams (`signals/fetchers.ts` GDELT-FAILED + NWS-degraded). `evals/logger.test.ts` (pure `resolveLogLevel` + wiring + a redaction regression test). `npm audit --audit-level=high` clean (no regression).
  - **C2 cross-instance idempotency — DONE + verified on real Postgres 17.** PG `saveDecisionPacket` reserves the idempotency key inside the persist txn (`.returning()` conflict-detect → `IdempotencyKeyConflict` → rollback → return winner), closing the F2 orphan race with NO schema change. Gated test `db-concurrency-integrity.test.ts` (same-key save race → 1 packet, no orphan, both callers get the winner). Throwaway-PG17 `test:db` GREEN (9 passed). Cross-instance double-WORK still bounded-by-budget/post-MVP (documented). Memory store unchanged (single-process).
  - **C3 style-src `'unsafe-inline'` drop — DEFERRED WITH REASON (do-no-harm).** Investigated: it's all-or-nothing (any inline `style=` attr keeps the requirement), and the live exposure bar (`action-packet-view.tsx` `.runway-fill` `width:${width}%`) is genuinely dynamic → eliminating it needs width-bucketing (precision loss on a core data-viz) or per-render nonced `<style>` (complexity in the white-screen-risk CSP area), for a gain guidelines-monitor rated "acceptable as-is" (style injection ≪ script injection; script-src is already strict-nonce). Recommend keep-deferred; owner can override.
  - **Bucket B (judge → cross-family FOLLOWED) — ✅ DONE 2026-06-22 (FREE, owner provided a Groq key).** `JUDGE_PROVIDER=groq` seam routes the judge to a non-Gemini **Meta** model (`meta-llama/llama-4-scout-17b-16e-instruct` default) via Groq's free tier (`lib/evals/judge.ts`: `resolvedJudgeProvider`/`judgeEnabled`/`makeGroqGenerate`; `@ai-sdk/groq@2`; `pricing.ts GROQ_PRICING` = Groq published rates). Few-shot critique-then-verdict prompt + held-out dev/test split + Cohen's kappa. **Calibrated: TPR 100% / TNR 100% / kappa 1.000** on the held-out split (free Groq calls; beat `gpt-oss-120b` = TNR 88.9% / kappa 0.889 / 10× slower → llama-4-scout is the data-driven default). The one VIOLATED best-practice is now **FOLLOWED**. ADR-0002 updated; README + Success_Criteria flipped. `npm run verify` unchanged (the live calibration stays `RUN_LIVE_AI_TESTS`-gated; verify needs no key + makes no Groq call). **Caveat:** the 18-item set is hand-authored synthetic prose, not real Dispatcher drafts — real-draft calibration is tracked (perfect score = necessary, not sufficient). **Codex cross-model: REVISE(3) → all fixed:** [High] Groq priced at $0 → **published rates** (groq.com/pricing, live-verified 2026-06-22: scout $0.11/$0.34, gpt-oss-120b $0.15/$0.60) so the ledger is honest on a paid key; [Med] disclose the small/synthetic set (done above); [Low] `GROQ_API_KEY` added to logger redaction + a `gsk_` secret-scan pattern. Routing/budget-before-call/fail-closed all APPROVED by Codex.
  - **Codex cross-model gate (C1+C2, batched):** REVISE(1 Med — logger redaction missed the `x-resilix-callback-secret` header + `DATABASE_URL`; F2 fix / no-deadlock / approval-atomicity-intact / test-non-vacuous all explicitly APPROVED) → fixed + redaction regression test → re-verify GREEN. Verdict weighed primary-model-final.

**DONE 2026-06-20 (local, push HELD; the two owner-requested "optional" items):** product-master allowlist (`936c6ef` — the existence grader now checks a real catalog `lib/data/product-master.ts`, not the run's own inventory) · nonce-based CSP (`d0eaf17`/`149f18d` — strict `proxy.ts` per-request nonce + `strict-dynamic`, drops `script-src 'unsafe-inline'`; `/`+404 smoke-verified nonced, error route nonced by the same client-component mechanism; Zod `jitless`). Locally gated (acceptance-gate + security-specialist + verify:full + prod browser smoke `scripts/prod-csp-smoke.mjs`). Evidence: `docs/claude/gates/optional-hardening-2026-06-20.md`.

**Codex cross-model gate on the optional-hardening commits — DISCHARGED 2026-06-20 (backup account):** REVISE(4: matcher `api`→`api/`, exposure-control test assertion, replay drift-guard source, stale comment) → fixed `d1d9200` → closure REVISE(1 comment nit) → fixed `8a44ae9`. Gate stack COMPLETE. Evidence in the gate doc.

**Security pass DONE 2026-06-20 (`84963e7`):** COOP + CORP (both same-origin) added to the static headers (verify:full GREEN + prod smoke confirms they ship + page renders under CORP). The CSP **report endpoint is DEFERRED WITH REASON** — a consumer-less report sink on an unmonitored, push-held portfolio artifact is theater + a new unauthenticated surface; the prod CSP smoke is the active drift detector. Codex BANKED for this increment (two pre-vetted static headers are below the cross-model threshold). Detail in the gate doc.

**Deferred (with reason, NOT silently dropped):** observability/pino + CODEOWNERS/PR-template (prod/team-scope) · deps-hygiene vite→esbuild `npm ls` warning (cosmetic; upstream fix) · the CSP report endpoint (above; add only if real prod monitoring lands). **Owner/local:** the manual screen-reader a11y pass.

**Pipeline checklist** (docs/PIPELINE.md):
- [x] **brainstorm** — direction owner-fixed (complete reorientation to ActionOps; uniqueness = action automation, not detection). Owner's autonomous-optimisation note assessed 2026-06-11: absorb financial-distress event type + 2am positioning + graduated-autonomy roadmap; reject prediction/sensors/auto-execution/demand-side for MVP.
- [x] **research** — codebase inventory done (LaunchOps salvage map); market validation done 2026-06-11 (pain REAL & SEVERE per BCI/McKinsey; white space = integrated loop under $20k for mid-market); US landscape research done 2026-06-12 → `docs/claude/RESEARCH-us-landscape-2026-06-12.md` (US-primary persona locked; demo = parameterized Hormuz chokepoint; 12-scenario shortlist; signal sources health-checked incl. new Census trade + FMCSA feeds; GDELT 3-month-window caveat). Remaining micro-items fold into build phases (GDELT query syntax → Sentinel phase).
- [x] **plan** — PLAN.md rev 3 locked 2026-06-12: grill resolved (Gemini ≤$5, seed+CSV tiered, local Postgres, on-demand, replay-first) → 3 interim adversarial rounds → **true Codex cross-model round (12 findings: driver swap to node-postgres, atomic approval/idempotency, DecisionPacketV2, full CSV ingestion spec + injection quarantine, mode-taxonomy split, claims[] gatekeeper, eval-control fix, source manifests) → closure round APPROVED — gate DISCHARGED, phases ≥2 unblocked.** Full argument in PLAN-REVIEW-LOG.md. Scope: 6 scenarios / 4 tabs / 2 live fetchers / 3 LLM calls+2 reserve / **~22 dev-days** honest estimate. **Owner sign-off pending.**
- [x] **resources** — working set selected by project-advisor 2026-06-11: skills = grill-with-docs-codex, agent-workflow-designer, next-best-practices, shadcn, database-designer, llm-evaluation, pw, fact-checker, documentation, ship-gate, design-review, humanizer · subagents = research-specialist, ai-engineering-specialist (reviewer), evals-specialist (judge), security-specialist (+ acceptance-gate per OS law).
- [x] **execution** — DONE + gated (all phases; see CURRENT STATUS above). 10 phases per PLAN.md (6 scenarios / 4 tabs / 2 live fetchers). **Per-phase discipline: every phase exits through acceptance-gate (whose panel includes the Codex cross-model leg); any phase whose approach deviates from PLAN.md gets a fresh grill-me-codex pass before code; the PLAN.md cross-model gate is DISCHARGED (Codex round 4 + closure APPROVED 2026-06-12) — phases ≥2 unblocked, owner sign-off GRANTED 2026-06-12.** One fresh session per phase (merge light adjacent phases), HANDOFF.md updated at each boundary. (exit gate: acceptance-gate)
  - [x] **Phase 1 — identity/spec rewrite** DONE 2026-06-12: README.md + docs/Success_Criteria.md rewritten to ActionOps; acceptance-gate (5 fixes) + Codex cross-model (7 findings → APPROVED) discharged; repo-wide identity reconciliation done (2 inline fixes, 22 banners, see docs/claude/PHASE1-doc-reconciliation.md).
  - [x] **Phase 2 — data model at scale** (~2.5d) — ✅ COMPLETE 2026-06-17 (P2.1–P2.7 all gated). Autopilot-driven 2026-06-13→17. Ordered increments, each exits through acceptance-gate (incl. Codex):
    - [x] **P2.1 — data-layer integrity (R4-1/2/3)** ✅ DONE 2026-06-13 (gated: verify green 24-passed/3-skipped + Codex cross-model APPROVED after 2 fix rounds; committed): swap drizzle-orm/neon-http → node-postgres (Pool; supports txn); atomic approval transition (compare-and-set: pg conditional UPDATE…WHERE approval_status='PENDING' RETURNING inside db.transaction + audit + event-mark in one txn; memory = synchronous guarded mutation, no await gap); reserve-then-run idempotency (in-process keyed mutex for MVP single-instance + key check). Tests: concurrent approve/reject → exactly 1 UPDATED + 1 CONFLICT; idempotent double-run → 1 packet, 0 duplicate LLM work. Keep in-memory fallback; pg path gated behind RUN_DB_INTEGRATION_TESTS. Free-stack: pg, drop @neondatabase/serverless.
      - Codex R1 fixes (accepted): (F1) reserve `processed_approval_events` row FIRST in pg txn — same eventId can't approve two packets concurrently; (F3) wrap pg saveDecisionPacket packet+key+projections in one transaction; (F4) 0-row branch re-reads the `approval_status` COLUMN (the guard) not the payload; (F5) add gated pg concurrent approve/reject + same-event/different-packet tests; (F6) strengthen idempotency test with an explicit single-execution spy.
      - **LOGGED LIMITATION (Codex F2, refuted for MVP):** cross-INSTANCE idempotency reservation is NOT implemented — the in-process mutex only serializes one Node process. ACCEPTED for the MVP (single-instance, on-demand, per PLAN.md "on-demand only" + multi-tenant out-of-scope). **Post-MVP (gated on multi-instance deploy):** DB-backed key reservation before pipeline work (nullable-packetId reservation row or advisory-lock pattern). Tracked here; surface in the expansion section at ship.
    - [x] **P2.2 — mode taxonomy split (R4-8)** ✅ DONE 2026-06-13 (gated: verify green 37-passed/3-skipped + Codex cross-model REVISE×5→APPROVED; committed db17010): agent_mode enum → LIVE_AI / DETERMINISTIC_RULES / REPLAY / FAILED_TO_FALLBACK + requested-vs-effective per packet; `!liveAiEnabled()`→DETERMINISTIC_RULES (healthy), live-attempt-failure→FAILED_TO_FALLBACK; parseStoredPacket back-compat shim (interim — superseded by P2.3 packetVersion).
    - [x] **P2.3 — DecisionPacketV2Schema + packetVersion (R4-7)** ✅ DONE 2026-06-13 (gated, committed as `Phase 2 / P2.3`). `npm run verify` GREEN (53 passed/3 skipped). `z.discriminatedUnion("packetVersion",[V1,V2])` (union = canonical `DecisionPacket` type); V2 = ActionOps shape (threatCard/exposureResults/simulation?/playbooks/supplierMessages+claims{value,unit,sourcePath}/actionItems/dataTier+dataGaps), NO options/executionDraft; pipeline stays V1; store version-aware (NOT-NULL exceptionId guard for V2; normalizer upgrades only version-less legacy → malformed versioned packets fail loudly); dashboard ActionOpsPacketView (full 4-tab = Phase 8); 16 new/changed tests inc. jsdom V2 render. **GATE DISCHARGED:** acceptance-gate substance-confirmed (BLOCK→em-dashes fixed); Codex cross-model R1 REVISE→fixed ([Med] normalizer fail-loudly +2 tests; [Low] V2-approve disclosed Phase-8) → **closure round APPROVED 2026-06-13, no new defects** (note: closure interrupted once by the Codex usage cap, re-run after the ~1:05 PM reset). Lesson logged.
    - [x] **P2.4 — schema extension** ✅ DONE 2026-06-13 (gated, committed dc67d06): additive ActionOps data-at-scale schema (pipeline stays V1, forward-laid for Phases 5/7/8). New tables products/chokepoints/routes/route_chokepoints(M:N)/disruption_events(persisted, survives GDELT window)/exposure_results/action_items/supplier_messages; suppliers gains nullable `sector`. **sector = text + Zod SectorSchema (incl. OTHER_UNMAPPED), NOT pgEnum** (Phase 4 owns/refines the closed vocab; avoids 0001-style enum-migration pain); country = text + alpha-2 CountryCodeSchema (app-layer ISO-3166). The 3 transactional projections each carry a **NOT NULL disruption_event_id FK** (event-keyed; the decision-packet payload stays canonical for packet-scoped output). **Genuine Zod<->DB alignment** (numeric {mode:"number"}, jsonb $type<...>, nullable -> Zod .nullish(); timestamptz stays Date-mode + Date->ISO domain mapper — mode:"string" emits the PG text format that z.string().datetime() rejects). products = single reconciled master (persisted type ProductMaster avoids the legacy operations.ts Product collision). Migration 0002 purely additive (8 CREATE TABLE + ALTER suppliers ADD sector; no DROP/ALTER TYPE). evals/schema-extension.test.ts = $inferSelect-typed alignment test with real teeth ($tsc-fails-on-drift + exact column sets + .notNull assertions). **GATE DISCHARGED:** independent verify GREEN (64 passed/3 skipped) + acceptance-gate SHIP + Codex cross-model REVISE(4: nullable FKs, false numeric/timestamp/nullable alignment, hollow test, Product collision) -> fix -> REVISE(1: mode:"string" timestamp not ISO) -> fix -> APPROVED (closure 2). 2 lessons logged.
    - [x] **P2.5 — CSV ingestion (R4-5/6)** ✅ DONE 2026-06-15 (autopilot; gated, committed): `app/api/suppliers/upload` over a framework-free ingestion core. papaparse; **byte cap ≤2MB BEFORE parse** (Content-Length then UTF-8 recount); **row cap ≤2000 via papaparse step+abort** (no unbounded array); **formula-injection sanitize** (leading `= + - @` apostrophe-escaped + both-ends whitespace trim closes the bypass) on every cell before ID/store/report; **canonical ID = `SUP-<sha256(normName|COUNTRY)[:16]>`** so the ID-quarantine (uploaded names never cross downstream raw) + dedup (same name+country → last-write-wins) align by construction; per-row matched/unmatched report with **zero-match-impossible enforced structurally by a `discriminatedUnion` report schema** (UNMATCHED requires a reason); Tier-2 cols → DataTier flag only (Tier-1 → `suppliers` table only; NO route/runway/inventory writes — Phase 4/5); dual pg/in-memory store (pg upsert `onConflictDoUpdate` in one txn); **auth deferred to P2.7 via a no-op chokepoint** (R4-4); `SupplierSchema` aligned to `suppliers.$inferSelect` (`.nullish()` sector/backupSupplierId). **GATE DISCHARGED:** independent `npm run verify` GREEN (100 passed/5 skipped) + **throwaway-Postgres 17 live suites GREEN** (5 passed: onConflict last-write-wins + $inferSelect round-trip on real pg) + acceptance-gate SHIP + Codex cross-model REVISE(4: **32-bit `stableHash`-as-PK collision [High]** (Codex-verified pair), header trim order, non-discriminated report schema, destructive db test) → fix → REVISE(1 [Low] whole-table length assertion) → fix → **APPROVE**. 2 lessons logged.
    - [x] **P2.6 — ~150-row US supplier seed** ✅ DONE 2026-06-15 (autopilot; gated, committed `c6494b4`). Tier-1 US-plurality showcase seed (150 rows) **designed backward from the locked demo scenarios** (Hormuz→9 Gulf-origin ENERGY/CHEMICALS; tariff §232/301→CN 11 in semis/elec/auto/metals + Asian semis 9; trucking→US-domestic plurality 71/LOGISTICS 7), flowing through the **P2.5 ingestion core** (`ingestSupplierCsv`: same sanitize / sha256 `SUP-` id / Zod) so the seed can't drift from the upload contract. **Tier-1-clean by decision** (advisor + Codex agreed): P2.5 doesn't persist Tier-2, and P2.4 normalizes routes/inventory into separate tables → Phase 4/5 seed those; a Tier-1 country/sector mix already drives the Hormuz exposure demo (PLAN item 5). Data is **synthetic/modeled** (no real-company risk claims; provenance in `data/seed/README.md`). **No-new-dep loader**: `lib/ingest/seed-suppliers.ts` (DI store; `ingestSeed()` fails loud on abort/unmatched/raw-count-drift/overwrite) + non-gated `evals/supplier-seed.test.ts` (exact per-sector/country/tier counts + `OTHER_UNMAPPED===0` + **scenario-critical country×sector JOINT cells** + raw formula-injection tamper guard + deterministic ids + Tier-2 detection fixture) + gated `evals/db-supplier-seed.test.ts` (additive+idempotent pg seed, full field round-trip) + `npm run seed:suppliers` (fail-loud DATABASE_URL preflight). **GATE DISCHARGED:** independent `npm run verify` GREEN (113 passed/5+3 skipped) + **throwaway-Postgres 17 `test:db` GREEN (8 passed)** (psql 150 / US 71 / Gulf 9 / 14 sectors) + acceptance-gate SHIP + Codex cross-model (thread `019ecd64-56f8-7452-b310-bb12890a1208`) **REVISE(4: [High] marginal-only test misses the country×sector matrix + 3 Med) → fix → closure APPROVE (no new defects)**. 1 lesson logged.
    - [x] **P2.7 — fail-closed APPROVAL_TOKEN + mandatory callback secret (R4-4)** ✅ DONE 2026-06-17 (autopilot; gated, committed). Fail-closed = DENY-on-missing-config: `secureModeRequired()` (DATABASE_URL || liveAiEnabled || REQUIRE_APPROVAL_TOKEN, all via robust `envBool`) gates the 3 mutation routes — upload + the **2 PREVIOUSLY-UNGATED** `approve` & `run-exception` (Codex corollary: a live-AI run is never authless) — on a bearer `APPROVAL_TOKEN` (length-oblivious SHA-256 compare; min-16-char; Bearer scheme required); `verifyN8nCallbackSecret` hardened mandatory in secure mode (no DEMO_UNCONFIGURED pass-through; AGENTS.md = hardened-not-extended). Pure in-memory demo stays authless + disclosed (`.env.example` mandates `REQUIRE_APPROVAL_TOKEN=true` for any shared/public demo). `liveAiEnabled` extracted to dependency-free `lib/server/env-flags.ts` (shared by run.ts + security.ts — no AI-SDK in the per-request auth path; drift structurally impossible). **GATE DISCHARGED:** `npm run verify` GREEN (138 passed/8 skipped) + acceptance-gate SHIP (gates 1/3/4/5) + Codex cross-model (thread `019ed40c`) REVISE(1 Med fail-open opt-in parse + 4 Low: drift, length-leak, weak-token, docs, n8n-test) → fix → closure **APPROVE** (no new defects). 3 lessons logged. **← Phase 2 COMPLETE (P2.1–P2.7 all gated).**
  - [x] **Phase 3 — signal layer, replay-first** (DONE — P3.1 + P3.2 gated 2026-06-17; see CURRENT STATUS above). GDELT DOC 2.0 + NWS, replay-first + resilient; each increment exits acceptance-gate + Codex. Grounded gap **P3 (G1): user-visible staleness marker** — data layer DONE (`gdelt.ts freshnessMinutes` + CACHED/stale status), UI surfacing → Phase 8.
    - [x] **P3.1 — GDELT DOC 2.0 fetcher** ✅ DONE 2026-06-17 (gated, committed `52a0f7e`; relay `1e3a09c`). Replay-first + never-throws-into-pipeline: ≥5s spacing, bounded per-scan cache (FIFO cap), 429/error backoff → fresh-enough cache or FAILED, max-serve-stale bound, in-flight coalescing; control-char-stripped fields + http(s)-only urls (shared `HttpUrlSchema` — every signal source inherits the no-`javascript:` guard); DI (fetchImpl + now) for deterministic tests; missing/malformed/far-future dates → worst-case-stale. **GATE DISCHARGED:** verify GREEN (171/8, 33 edge tests) + acceptance-gate (1 BLOCK: DRY the stale-bound + test the refusal branch → fixed) + Codex REVISE(9)→rework→closure REVISE(2)→fix→APPROVE. 3 lessons logged; anti-AI comment pass applied (WHY-not-WHAT, ASCII, no tells).
    - [x] **P3.2 — GDELT-primary signal layer + dated ActionOps replay + `verify:live`** ✅ DONE 2026-06-17 (gated, committed `3a5324e`→`bf4f4ee`). GDELT DOC 2.0 made the PRIMARY live signal (via the P3.1 `fetchGdeltSignals`); NWS kept live (DI `fetchImpl`/`now`); USGS+EONET demoted to fixture-only; **Open-Meteo dropped**. `cached.ts` reworked to dated (2026-06-17) ActionOps fixtures — the GDELT replay set DERIVED through the shared `mapGdeltArticles` (no drift, golden-id-pinned). Shared `sanitize.ts` trust boundary across GDELT+NWS (Law 11); a GDELT outage surfaces a schema-valid **FAILED disclosure marker** (never silent); malformed/far-future dates → worst-case-stale; NWS-fail serves CACHED-never-LIVE. New `verify:live` smoke (gated, NWS-LIVE anchor) + `record:signals` recorder; coverage floor ratcheted 77/76/74/65. **GATE:** verify 183 passed/10 skipped + coverage above floor + verify:live 2 passed (live) + acceptance-gate SHIP + Codex REVISE(4)→fix→closure REVISE(4: NWS sanitize/freshness/id + drift teeth)→fix→re-verified GREEN; confirm rubber-stamp infra-banked (3 codex runs emitted no verdict — tooling). 4 lessons logged. Evidence: `docs/claude/gates/signal-layer/p3.2-2026-06-17.md`.
- [~] **deployment — PREP DONE 2026-06-24; the actual hosted deploy is the one OWNER action left.** `verify:full` GREEN first-hand (515 unit / 21 skipped + **14 e2e** + build + secrets). Sub-parts closed: real-HTTP upload e2e DONE (`evals/e2e/supplier-upload.spec.ts`, via `test:e2e`); README ActionOps thesis clean (the single "LaunchOps" mention is deliberate predecessor provenance, NOT an identity remnant); deploy story `docs/deploy.md`; **current demo walkthrough `docs/demo.md`** (key-off deterministic; supersedes the stale archived `docs/_archive/demo_script.md`). The "recorded demo run" as a DATA artifact already exists — the frozen REPLAY on `/` (`lib/data/demo-packet.ts` + the golden fixtures), $0 / no-key. **REMAINING (owner-gated / sequenced, NOT finishable here):** (a) the actual hosted deploy — **owner-SEQUENCED behind design (owner directive 2026-06-24): owner finalizes design FIRST, THEN Claude deploys**; hosting choice + flip `REQUIRE_APPROVAL_TOKEN=true` for any shared demo (secure-by-default already lands in prod). Do NOT deploy before design is finalized; (b) an optional screencast (needs a human); (c) the manual screen-reader a11y pass (`docs/claude/A11Y-MANUAL-SR-PASS.md`). The `acceptance-gate` exit fires WHEN the owner greenlights hosting — do not run it speculatively. (exit gate: acceptance-gate, at deploy)

**Safety invariants (Law 11 / trifecta):** GDELT article text is untrusted data, never instructions, in every agent prompt · outbound supplier emails exist only as drafts until human approval · quarantine legacy `resilix_pipeline_v2*.json` + `prompts/` as reference data.

---

## Component D — the ActionOps agent core (Phases 4–7) — Wave-2 (autopilot, started 2026-06-18)

> **The heart of the rework.** REBUILD `runLaunchOpsAgents` (emits V1) → the 6-agent ActionOps pipeline emitting **V2**; BUILD-NEW the `claims[]` gatekeeper, the cost ledger (R4-10), and the injection quarantine. **The executable contract = the 7 frozen F graders** (`lib/evals/run-graders.ts`; built + gated `e84b1fc`→`720ff25`) — `runGraders(packetV2, groundTruth)` runs unchanged over D's live output. The complete V2 shape reference is `lib/data/demo-packet.ts` (`makeDemoPacket()`, real seed suppliers).
>
> **Owner decisions (2026-06-18):** autopilot drives **D.1–D.8 (pre-key, deterministic, key-OFF) to full gated completion, then STOPS at D.9** (the live-AI showcase — owner-gated: external paid calls + secure-mode auth). The Gemini key is treated **UNCONFIRMED** (anomalous format) — no live call until the owner confirms/replaces it ("I will give the key"). `ENABLE_LIVE_AI=false` throughout the spine.
>
> **Plain English.** Right now the app's pretty screen shows a *canned* example packet, and the real engine still produces the old LaunchOps format. Component D rebuilds the engine as six specialists — one reads the news (Sentinel), one checks it (Verifier), one maps which of *your* suppliers are exposed (Atlas), one does the runway math (Simulator), one writes the plan (Strategist), one drafts the emails with every number traceable (Dispatcher) — and wires the real engine's output into that screen. We build and prove the whole deterministic spine with the AI turned OFF first, gate each step, and only turn the paid AI on at the very end, which is your call.

**Each increment (invariant):** maker → independent `npm run verify` → `acceptance-gate` (subagent) → **Codex cross-model** (`~/claude-os/bin/codex-guarded`, `-resilix`-namespaced, `< /dev/null`) → local commit. Push HELD. Evidence to `docs/claude/gates/agent-core/`. Mirror Phase 2's P2.1–P2.7 discipline.

| # | Increment | Key? | Maps to / contract |
|---|---|---|---|
| D.1 | **✅ DONE 2026-06-18 (gated, `d5e18a8`→`03375f2`) — V2 cutover skeleton (keystone).** `runExceptionPipeline`=`buildDecisionPacket`+save+idempotency (emits V2 via the deterministic 6-agent `runActionOpsAgents`); `/api/run-exception` + the `/` render path (async server component, force-dynamic) cut over to the real pipeline packet; V1 "Run live pipeline" panel + `launchops.spec` retired (Option A); ~7 V1-asserting tests migrated + `makeV1Packet()` oracle; `runGraders` green end-to-end key-OFF. Gate: verify 300/10 + e2e 11 (a11y on the REAL packet) + acceptance-gate(4+5 PASS) + Codex REVISE(8)→5-fixed/3-tracked→closure REVISE(1)→fixed→closure-2 APPROVED. Evidence: `docs/claude/gates/agent-core/D1-2026-06-18.md`. | OFF | R4-7; emits `DecisionPacketV2`; preserves mode taxonomy + `computeEffectiveMode` |
| D.2 | **✅ DONE 2026-06-18 (gated, `6579b31`→`10d5f04`) — Atlas** deterministic exposure scoring → `exposureResults`. `score = RISK_TIER_BASE[tier] + clamp(leadDays-30,0,30)` (integer, no float fragility); Sentinel-handoff firewall (chokepoint→affected-scope map; misclassification OR claimed-but-unmapped → **fail closed**, gatekeeper BLOCKS on agent FAIL); sector firewall (→OTHER_UNMAPPED); zero-exposure ("no direct exposure"); 9 hand-DERIVED fixtures. Gate: verify 310/10 + e2e 11 + acceptance-gate SHIP(1/4/5, re-derived all 9 scores) + Codex REVISE(3: firewall-failed-OPEN/exact-string-bypass/demo-disclosure)→fixed→closure APPROVED. Evidence: `docs/claude/gates/agent-core/D2-2026-06-18.md`. | OFF | `gradeExposureControl` + zero-exposure + off-taxonomy SECTOR; hand-computed fixtures; **deliberate-misclassification Atlas-rejection assert** (DONE) |
| D.3 | **Simulator** (deterministic exact arithmetic → `simulation`; Tier-1→`dataGaps`) | OFF | `gradeSimulatorArithmetic`; **product-master allowlist** swap (F-deferred) |
| D.4 | **Verifier** (DISTINCT deterministic agent + own `AgentRun`) + **ActionOps gatekeeper** (`claims[]` bidirectional numeral↔sourcePath; replaces the LaunchOps `validateDecisionInputs`) | OFF | `gradeCitationCoverage`; wrong-context-number FAILS; "healthy never mislabeled" keeps Verifier/Atlas/Sim/gatekeeper as separate `DETERMINISTIC_RULES` runs |
| D.5 | **Sentinel** (LLM #1 → `threatCard`; closed vocab + `OTHER_UNMAPPED`; ONLY agent that sees raw text; names→validated ID; evidence ∈ fetched set) — built with deterministic fallback, tested vs fixtures, run key-OFF | OFF (live at D.9) | `gradeEntityIds`/`gradeEvidence`/`gradeOffTaxonomy`/`gradeInjectionQuarantine` |
| D.6 | **Strategist** (LLM #2 → `playbooks`; every number from Atlas/Sim; zero independent estimates) — fallback + fixtures, key-OFF | OFF (live at D.9) | **playbook-step numeral grading** (F-deferred) |
| D.7 | **Dispatcher** (LLM #3 → `supplierMessages`+`claims[]`+`actionItems`; NEVER sees raw text; top-5+tail; whitelist only) — fallback + fixtures, key-OFF | OFF (live at D.9) | P7 dispatcher-whitelist + injection eval; `gradeInjectionQuarantine` |
| D.8 | **Cost ledger (R4-10)** — input/output/total tokens, finish reason, retry/error class, pricing-table version, computed `costUsd` (replaces `length/4`); `verify:live` wiring; runGraders as the live-output gate; finalize F-deferred items | OFF (verified at D.9) | Success_Criteria cost/usage row; ≤$5 ledger |
| **D.9** | **✅ DONE 2026-06-19 (gated; owner released the gate, "go live now").** Live AI wired into the pipeline (async orchestrator routes to the 3 LLM bodies when `live && liveAiEnabled()`, cumulative per-run budget; provider-key bug fixed GEMINI_API_KEY→createGoogleGenerativeAI; preflight ListModels). ALL 6 scenarios (7 records) built as PRODUCTION scenarios (only Hormuz existed) + scenario-keyed replay signals → each classifies coherently live (closes "6 of 6 coverage"); `ThreatEventTypeSchema` vocab expanded (+ROUTE_DIVERSION/TARIFF_DEADLINE/SUPPLIER_BANKRUPTCY); Atlas match `regions`/`riskTiers` + Suez scope. G-5 judge built + calibrated (TPR/TNR ≥ 0.83, fail-closed) + WIRED over live drafts. Live eval `actionops-live-real` (6 scenarios: modes + graders + judge) + `judge-calibration` + `record-live-packets` (fixtures `evals/fixtures/live/`). **GATE:** verify:full GREEN (453/19 + build + secrets + 11 e2e/a11y) + acceptance-gate(1/3/4/5 PASS) + **Codex cross-model REVISE(6)→fix-5/doc-1 → closure REVISE(4)→fix-3(#1 judge billing self-guard, #2 numeric-name mask, #4 requestedMode)/refute-1(#3 event-location≠supplier-location, evidence) → closure-2 cap-interrupted (re-run after reset)**; SHIP primary-model-final. Metered spend ≈ $0.8–1.0 (<$5). Evidence: `docs/claude/gates/agent-core/D9-2026-06-19.md`. Deferred/tracked → **ALL 4 DONE 2026-06-19** (see the follow-ups row below). Also: competitive-gap analysis (`docs/competitive-gap-2026.md`), README repositioned to the trust spine, 9 legacy files removed. | **ON** | DONE — owner released; live showcase complete |

**Post-D.9 follow-ups — ✅ ALL 4 DONE 2026-06-19 (gated; push HELD).** One batched checkpoint (`f9689c0`→`0f3fe7d`):
1. **Explicit NO_ACTION refusal packet** — `decideRecommendation` (`!corroborated` [now DISTINCT-source] + `confidence < 0.45` [the UI "low" band] + a real-sector exposure → emit NO_ACTION, WITHHOLD playbooks + drafts, list `missingEvidence[]`). `SCN-THIN-EVIDENCE` = the demo's "Run B". The discriminator is unverified-not-count (hurricane/bankruptcy single-but-authoritative still ACT). **Live-verified: NO_ACTION @ Sentinel confidence 0.10.** Schema additive-optional + a union `superRefine` enforcing the refusal invariant.
2. **Home → frozen live REPLAY** — `lib/pipeline/replay-packet.ts` serves the captured Hormuz live packet relabeled REPLAY ($0, static, never "live", fail-loud on schema OR non-live-capture drift).
3. **Bounded retry (+2 reserve)** — `liveGenerateValidated` + a SHARED run-level `makeRetryReserve` threaded to the 3 agents; ≤5 calls worst-case; EXACT aggregated ledger across attempts.
4. **Codex closure-2** — re-confirmed (B1 judge self-guard, B2 numeric-name mask, B3 requestedMode, B4 event≠supplier-location all HOLD).

**GATE:** `npm run verify` GREEN (478/20) + `test:e2e` 12 + gated LIVE 8-scenario GREEN + 360-review (security CLEAN · evals real-teeth · ai-eng SHIP · acceptance-gate BLOCK→doc-fixed) + Codex REVISE(6)→fix-4/refute-2, BLOCKER-2 **refuted by live evidence** (conf 0.10), closure cap-interrupted (re-run ~1:39 AM Jun 20 offered, belt-and-suspenders). **SHIP primary-model-final.** Metered live spend this session ≈ $0.2 (build total ≈ $1.0–1.3 < $5). Evidence: `docs/claude/gates/agent-core/FOLLOWUPS-2026-06-19.md`.

**MODEL POLICY (owner 2026-06-18: "latest at lowest cost + never retired"; live-verified via ListModels on the real key — 2.5 is the latest gen, NO gemini-3.x exists):**
- **COST-VERIFIED ALLOCATION (live pricing 2026-06-18; 2 sources agree — ai.google.dev/gemini-api/docs/pricing + pricepertoken.com).** Std per-1M tokens, GA + ON THE KEY: `2.5-flash-lite` $0.10/$0.40 · `2.5-flash` $0.30/$2.50 · `2.5-pro` $1.25/$10. 3.x EXISTS on the official page (`3.5-flash` GA $1.50/$9 · `3.1-flash-lite` preview $0.25/$1.50 · `3.1-pro-preview` $2/$12 · `3-flash-preview` $0.50/$3) but is **NOT enabled on this key** (live ListModels = 2.5 only → the earlier "gemini-3.5-flash doesn't exist" was imprecise: it's REAL but an ACCESS issue). Our WHOLE live run (≤3 passes + ~50 judge items) is **<~$1.5 on ANY model** → cost is NOT a constraint. **DECISION (owner rule: small delta → use best/latest-available; cost negligible → choose by quality):** default the LLM agents to **`gemini-2.5-flash`** (best-value GA, ~$0.3-0.5/run) — NOT flash-lite (savings are pennies, flash is materially better); step the citation-strict **Dispatcher → `gemini-2.5-pro`** only if D.5/D.9 evals show flash misses citation faithfulness; **judge ≥ agents** (pro); `flash-lite` only for trivial Sentinel classification if an eval proves it suffices. **3.x option (owner action, NOT blocking):** to use the latest `gemini-3.5-flash`, enable 3.x access on the AI Studio project + re-verify ListModels — but it is pricier than 2.5-flash and most 3.x are PREVIEW (conflicts with never-retired/GA), so GA `2.5-flash` is the recommended sweet spot.
- **QUALITY-GATED ASSIGNMENT (owner 2026-06-18, reinforced twice): the model is ASSIGNED per agent by MEASURED output quality, not guessed.** The eval IS the quality check — F's deterministic graders (citation faithfulness · entity+URL existence · off-taxonomy) + the D.9 judge (no-unsupported-claims) score each agent's REAL output; default is `gemini-2.5-flash`; the eval moves an agent UP to `gemini-2.5-pro` (if flash misses the bar) or DOWN to `gemini-2.5-flash-lite` (if a cheaper tier already passes). Measured at D.5 (fixtures) + D.9 (live + judge), where output exists to score (the key-OFF spine generates none). Record each agent's chosen model + its quality evidence in the D.8 ledger / the D.9 gate artifact.
- **Never silently retired:** keep the model a single `GEMINI_MODEL` config point + add a **preflight ListModels check** at live-AI startup that FAILS LOUD (lists the current set) if the configured model is unavailable → a Google retirement = a one-line config bump, never a silent mid-run 404/wrong-model fallback. CHOSEN OVER the `-latest` auto-rolling aliases on purpose: `-latest` never retires but silently shifts behavior + token pricing under the frozen golden records + judge calibration + ≤$5 ledger (breaks reproducibility). Pinned-GA + preflight = retirement-safety AND stability. Lands at D.5 (default + preflight) and D.8 (pricing).
- FIX the stale `gemini-3.5-flash` default in `lib/agents/run.ts` → `gemini-2.5-flash` at D.5 (GA + on the key; NOT flash-lite — cost is negligible so default to the better tier per the owner's small-delta rule). `gemini-3.5-flash` is REAL on the official page but NOT enabled on this key (access, not non-existence).
- **COST (owner 2026-06-18: "free first, then cost, keep under $5"; key is pay-as-you-go/billing-enabled):** the deterministic spine **D.1–D.8 is $0** (no live calls — fixtures / replay / deterministic). Only **D.9 bills:** cheapest model (`gemini-2.5-flash-lite`), ≤3 passes; the **cost ledger (D.8) sums real tokens × the pinned price row and HARD-STOPS before $5** (fail-closed if a call would breach the cap). Use any free-tier quota first; overage bills pay-as-you-go — the ledger is the guard, not a hope. This is the Success_Criteria "≤$5 total LLM spend" row, enforced.

**Other verify-before-build owed before D.5 (spine-independent; kick lean/async at D.4→D.5):** free-tier RPM/RPD + the matching **price-table row** live-verified at D.8 (the ledger `costUsd` is wrong if the pinned price ≠ the alias); constrained-decoding force-fit vs parse-fail (settles the `OTHER_UNMAPPED` firewall); current OWASP LLM Top-10 2025 + Agentic Top-10 2026 names + Willison lethal-trifecta + Meta Rule-of-Two. **NLI faithfulness (G-7) is research-derived, NOT a Success_Criteria gate → deferred HARDEN** (the real bar is `claims[]`↔`sourcePath`, which F already gates); decide explicitly before adding the DeBERTa-class local-model dependency.

**D.1 Codex-round deferred items (tracked 2026-06-18, NOT dropped — Codex REVISE(8): 5 fixed in D.1, 3 tracked here, all weighed by primary-model-final):**
- **→ D.4 (Verifier): relevance-filtered threat corroboration.** D.1's deterministic Verifier counts every fetched signal as backing the threat; the UI copy was made honest-descriptive now ("Across N recorded sources", not "Corroborated") + the computed `evidenceAllowlistPassed` line stays. The REAL fix — only count threat-relevant evidence, verify `threatCard.evidenceUrls` against the fetched source manifest, separate "recorded context signals" from "cited threat sources" — is D.4's bidirectional `claims[]`↔`sourcePath` Verifier. (Codex #1 High.)
- **→ D.5 (mode honoring): `requestedMode=LIVE_AI` must reflect a real attempt.** `buildDecisionPacket` sets `requestedMode = liveAiEnabled() ? LIVE_AI : DETERMINISTIC_RULES`; correct in D.1–D.8 (key-OFF → always DETERMINISTIC_RULES), but once the LLM agents land they must actually attempt live and emit `LIVE_AI`/`FAILED_TO_FALLBACK` from the real attempt (not just from the global flag). Add a key-ON provenance test at D.5. (Codex #6 Med.)
- **→ post-MVP (already P2.1-logged): cross-instance idempotency is not authoritative.** The in-process mutex protects one Node process; in Postgres `saveDecisionPacket` writes the packet before the idempotency-key insert (`onConflictDoNothing`), so two instances on the same key can each persist a packet (one orphaned). This is the documented single-instance-MVP limit (`lib/pipeline/run-exception.ts` comment + HANDOFF P2.1: "cross-instance DB reservation = logged post-MVP item"). The fix = reserve/lock the key before assembly (or detect the insert conflict in-txn, roll back, return the winner) + a DB same-key concurrency test. (Codex #4 Med — preserved verbatim by D.1, not introduced.)
- **NO ACTION (Codex #8, refuted): seed CSV cwd path.** `SEED_CSV_PATH = path.join(process.cwd(), ...)` is correct-by-construction for Next's run model (cwd = project root under `next build`/`dev`/`start` and vitest); `readFileSync` already throws `ENOENT` with the attempted path, satisfying "fail with the path." Codex's `process.chdir("app")` is artificial — nothing runs the app from a non-root cwd. Env-override + cwd-drift test = gold-plating.

## Carried backlog — durable, nothing lost (thread-swept 2026-06-17)

> Everything surfaced in-session that is NOT yet done, captured here so a cap-cut loses nothing. State · where it binds. (Closes the "lost in chat" risk the owner flagged 2026-06-17.)

### Industry-grade hardening (the craft baseline a top shop ships)
- [x] **H2 — `.editorconfig` + `.nvmrc`** ✅ DONE (`ca70781`).
- [x] **gdelt.ts anti-AI comment pass** ✅ DONE — WHY-not-WHAT, ASCII `--`, no prose tells (verified 2026-06-17).
- [x] **H3 — test coverage tooling** ✅ DONE — `@vitest/coverage-v8` + `npm run coverage` + a ratcheted floor (now 82.18/72.07/80.37/82.43; gated in F + component G).
- [x] **Component G — 3-layer a11y CI + G-3/G-4 + SCA** ✅ DONE 2026-06-18 (gated, `c676f46`→`61f7099`). `@axe-core/playwright` axe WCAG 2.2 AA over all 4 tabs + approved state, APG keyboard + SC 2.4.11, SC 2.5.8 target-size, SC 1.4.11 bar contrast (ground-truth sRGB via in-browser canvas) — pointed at the REAL `/` surface (HANDOFF "add a demo route" note was STALE; Phase 8 already wired V2 in). Caught 3 real AA fails Phase 8's self-claim missed. Manual SR pass = owner action (`docs/claude/A11Y-MANUAL-SR-PASS.md`). SCA cleared the 4 HIGH npm-audit findings (non-breaking). Gate: verify 297/10 + e2e 11 (V1+10 a11y) + coverage floor + audit-high 0 + acceptance-gate(inline, subagent infra-died) + Codex REVISE(6)→fix→closure REVISE(3)→fix→round-3-confirm. Evidence: `docs/claude/gates/a11y/G-2026-06-18.md`. **DEFERRED (tracked, not silent): vite→esbuild `npm ls` ELSPROBLEMS (pre-existing, `npm ci`-passing) → a dedicated deps-hygiene increment.**
- [ ] **H4 — Prettier** — config matched to the existing 2-space style + a `format` script. DEFER the mass-reformat (huge diff; its own increment).
- [ ] **Pre-commit hooks** — husky + lint-staged. Nice-to-have (CI + the baton partly cover). Post-MVP.
- [ ] **Observability** — structured logging (pino/winston); the fetchers/pipeline log via `note` fields only today. EXPANSION-PATH (prod).
- [ ] **CODEOWNERS / PR-template / CONTRIBUTING** — only if it goes team/open. EXPANSION-PATH.

### The 5 guidelines-monitor must-fix gaps (G1, 2026-06-17 — apply inline per phase, each gated)
- [ ] **P7 (highest)** — enumerate the Dispatcher whitelist; NO Sentinel `event_summary`/`affected_regions` free-text laundered into drafted supplier emails; add an injection eval (OWASP LLM01/05). → **Phase 7**.
- [ ] **P9** — judge calibration (N=20–40, TPR/TNR) + judge-error **fail-closed**. → **Phase 9** (G-5, key-gated; the ONE truly LLM-dependent eval, waits on `GEMINI_API_KEY`).
- [x] **P9** — a golden-task regression is a hard **BLOCK** at the gate (G-8). ✅ DONE 2026-06-17 (component F, gated `e84b1fc`→`720ff25`): `evals/golden-tasks.test.ts` runs `runGraders` over 7 frozen golden records inside `npm test`→`verify`; 22 corrupted twins prove every grader BLOCKs. Deterministic injection eval (G-6) built alongside (`evals/injection.test.ts` + `lib/evals/graders.ts gradeInjectionQuarantine`; structural-invariant primary grader). The P7 Dispatcher-whitelist half + the LLM-judge secondary stay → Phase 7 / G-5.

### F-deferred Success_Criteria rows — bound to D (the agent core, owner-gated on `GEMINI_API_KEY`), tracked here so they are NOT silently dropped (lesson F-2)
- [ ] **Deliberate-misclassification grader** ("a hand-corrupted Sentinel output fails Atlas validation visibly") — needs the Sentinel→Atlas handoff schema + Atlas validation fn (the agent core). The off-taxonomy SECTOR grader is the pre-key portion (built in F); the handoff-rejection assert lands when **D** builds Atlas. → **Phase 4/5 (D)**.
- [ ] **Playbook-step prose numeral grading** — F grades supplier-message numerals; playbooks currently ground via ids (`groundedClaimIds`), not inline `claims[]`. When **D**'s Strategist emits playbook prose with figures, extend `gradeCitationCoverage` to playbook steps (or require inline claims there). → **Phase 7 (D)**.
- [x] **Product-existence vs a real master allowlist** ✅ DONE 2026-06-20 (`936c6ef`): `lib/data/product-master.ts` is the authoritative catalog; `KNOWN_PRODUCT_IDS` derives from it (the `KNOWN_SUPPLIER_IDS` ← `ingestSeed()` pattern); the 3 ground-truth sites now check the catalog, not the run's own inventory. Run-scoped check stays in `gradeSimulatorArithmetic`. `evals/product-master.test.ts` = enumerate-every-scenario-product ∈ master + PROD-FAKE ∉ master.
- [x] **Calibrate the citation/numeral grader against REAL Dispatcher drafts** ✅ VALIDATED 2026-06-24 (owner provided `GEMINI_API_KEY`). Ran the gated D.9 live eval (`evals/actionops-live-real.test.ts`, `gemini-2.5-flash`) — **all 8 production scenarios genuinely live (Sentinel+Strategist+Dispatcher=LIVE_AI, `totalCostUsd>0`), within the $5/scenario cap, <5 min**; `runGraders(...).blocked === false` on every real draft → the F grader conventions (full figures / `$5M`-with-space / no scientific notation / `FIELD_UNITS`) raised **no false-positive block** on genuine Gemini output. Thin-evidence scenario correctly refused live (`NO_ACTION`, empty `supplierMessages`, `missingEvidence>0`). Caveat: 8 scenarios × live runs = a real but not large-N sample (the per-scenario assertions are deterministic-ground-truth, so they keep teeth). **Actual measured cost (console-visible re-run 2026-06-24): $0.0284 TOTAL across all 8 scenarios** (gemini-2.5-flash; max $0.008/scenario on TARIFF) — ~180× under the $5/scenario cap; full run ~4 min. Distinct from G-5 judge calibration (already done via Groq).
- [~] **P3** — user-visible **staleness marker**: data layer DONE; UI surfacing → **Phase 8** (the design workflow must render CACHED/stale + the degraded badge).
- [ ] **(low)** — cost-ceiling note IF scheduled scans ever ship (Law 8). → **Phase 10 / expansion**.

### P2.7 security residuals (security-specialist 2026-06-17 — all expansion-path, none block)
- [ ] callback-secret min-length · [ ] rate-limiting on the mutation routes · [ ] security headers (CSP/HSTS) · [ ] an n8n positive-path test. → **Phase 10 / the expansion section at ship**.

### Grounded roadmap binding (G3/G4 — framework + canon per phase)
Frameworks: **gsd-core** (spec-driven backbone, every phase) · **superpowers** (anti-slop + verify-before-completion, every increment) · **gstack** (taste/ship bar, Phase 8 + ship) · **Kashef** execution-shapes (per stage).
**External canon — adapt-the-best, mapped to the SURFACE. The lists below are a FLOOR, not a ceiling (a starting set, NOT exhaustive — "don't limit to it", owner 2026-06-17): each phase ALSO runs a live ≥3-source / ≥2-platform sweep + an unknown-unknowns scan, internal AND external, end-to-end, never from memory. (Multi-Source Mandate; vet→improve→adopt, never copy; ENFORCED by `project-advisor` + `guidelines-monitor` at each gate, drawing on `knowledge/source-registry/` + the KBs `agent_building_kb`/`evals_kb`/`harness_engineering_kb` + a live web sweep; procedure = `docs/EXTERNAL-ADOPTION.md`):**
- **LLM / agent phases (4–7):** Anthropic *Building Effective Agents* + tool-use/structured-output · Google *Agents* whitepaper + ADK + `google/skills` · Microsoft `agent-framework` + `ai-agents-for-beginners` · OpenAI *Agents SDK* + cookbook · LangChain / **LangGraph** patterns · `awesome-generative-ai-guide` · **OWASP-2025 LLM Top-10** (LLM01/05 injection) · **Willison lethal-trifecta** · **Meta Rule-of-Two** · Gemini structured-output. (→ P7 dispatcher-whitelist + injection-eval; P9 judge-calibration + golden-task BLOCK.)
- **Phase-8 UI / frontend:** **WCAG 2.2 + ARIA Authoring Practices (APG)** · Vercel / **Next.js** + React best-practices · Material 3 / Apple HIG where apt · the **anti-AI / human-craft** canon (no slop tells) · Refactoring-UI taste. (→ enforced at the design gate: `guidelines-monitor` + design-review + anti-slop + a11y + Codex.)
- **Cross-cutting (every phase):** Fable Mindset (GROUND→REASON→ACT→OBSERVE→RE-EVALUATE→VERIFY→NARRATE) · the 12 Laws + Rule 0 · maker≠judge + the mandatory cross-model Codex gate. Source: the G1 guidelines-monitor report.
