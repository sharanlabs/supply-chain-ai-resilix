# Component F gate evidence — deterministic eval harness (G-6/G-8)

**Increment:** Phase 9 / component F (ALIGNMENT-AUDIT). **Date:** 2026-06-17. **Maker:** sharank98 (Opus 4.8), owner-away autonomous mode, full permission.

## What F is
The agent core that PRODUCES decision packets is owner-gated (no `GEMINI_API_KEY`), so F builds the **deterministic graders + frozen golden records NOW** as the executable contract that core must satisfy. The SAME graders (`runGraders`, the organ-8 seam) run over the live pipeline's V2 output once it exists. Pre-key this is a spec/contract harness, labelled as such throughout (`run-graders.ts` states the post-key activation explicitly). Scope is the key-free half (G-6 injection eval + G-8 golden-task BLOCK); the LLM judge (G-5), NLI (G-7), the runtime gatekeeper (D), and live mode are owner-gated / Wave-2 and are NOT in F.

## What was built
- `lib/evals/source-path.ts` — total (never-throw) `sourcePath` resolver (dotted + `[n]`, prototype-key guarded), grammar inherited from the V2 fixture.
- `lib/evals/numerals.ts` — sourceable-numeral extractor: K/M/B magnitudes, masks ISO dates + slash ratios + uppercase ids, FAIL-CLOSED on scientific notation, left+right token boundaries (`50USD`/`123ABC`/`v2.1` not misread; `top-5` count kept).
- `lib/evals/graders.ts` — 7 graders: entity-ids (supplier + product existence), evidence (every url vs the fetch allowlist + internal ref integrity), citation-coverage (bidirectional numeral↔sourcePath + unit-consistency), off-taxonomy (closed-vocab + OTHER_UNMAPPED-not-force-fit), exposure-control (Atlas matching + zero-exposure, no invented score), simulator-arithmetic (exact recompute + runout symmetry), injection-quarantine (normalized no-raw-text over all drafted prose + structural id/url invariants). Each labelled `teeth-now` vs `regression-lock`.
- `lib/evals/run-graders.ts` — the eval-runner / hard-BLOCK aggregator (organ-8 seam).
- `evals/golden/` — builder + 7 frozen golden records (6 scenarios, ids derived from the real `ingestSeed`), 22 corrupted twins, parameterized injection payloads, frozen literal id snapshot.
- `evals/golden-tasks.test.ts` (the merge-BLOCK), `evals/injection.test.ts`, `evals/eval-graders.test.ts`.

## Verify (independent, offline-deterministic) — GREEN
- `npm run verify` -> **294 passed / 10 skipped**, exit 0 (typecheck + lint + test + build + secrets all clean). Evidence: `/tmp/resilix-F-verify.txt`.
- The **10 skips are DB + live-network INTEGRATION tests** (`db-*.test.ts`, `live-signals.test.ts`) gated on env flags (`RUN_DB_INTEGRATION_TESTS` / `RUN_LIVE_SIGNAL_TESTS`) — NOT the F graders, which all run. (Acceptance-gate confirmed this distinction; the key-gated AI graders are absent-by-design, not skipped.)
- `npm run coverage` -> exit 0. lines 82.2 / stmts 82.0 / funcs 79.8 / branches 72.0 — above the floor (80/80/78/69, ratcheted this increment from 77/76/74/65). `lib/evals/*` is 98-100% covered.
- The hard BLOCK runs inside `npm test` -> `verify`: every grader is proven by >=1 corrupted golden twin that BLOCKs; independent hand-computed arithmetic anchors (Hormuz $630k/$2.7m/$2.7m/2026-07-12; Red Sea; hurricane) defeat the f(x)===f(x) trap.

## Gate review — round 1

**acceptance-gate (independent judge): BLOCK (4 route-back).** All valid; all fixed:
1. [High] product-entity fabrication ungraded AND undisclosed (Success_Criteria.md:31 requires zero fabricated *product* entities); the simulator runout loop checked only `expected ⊆ actual`, so a fabricated runout passed silently — asymmetric with the horizons count guard.
2. [High] runout loop lacked the count + `actual ⊆ expected` symmetry guard.
3. [Med] no corruption twin for a fabricated/extra runout.
4. [Low] the citation reverse-check comment overclaimed (said "every numeral has a backing claim"; actual check is "a claim of equal value exists").

**Codex cross-model (`codex-guarded`, `-resilix`, fresh exec): REVISE (10 findings + fan-out sub-reviewers).** Primary-model-final disposition — 8 fixed/improved, 2 disclosed-with-rationale:
1. [High] same-value/wrong-context citation -> FIXED (unit-consistency via a FIELD_UNITS map; same-value/wrong-field fails on the unit) + corruption; same-value/same-unit residual DISCLOSED in-code -> G-5.
2. [High] lossy numerals ($1.2M, 1e6, 50USD) -> FIXED (K/M/B, fail-closed sci notation, left+right boundaries) + tests.
3. [High] product entities ungraded -> FIXED (knownProductIds + runout symmetry) + 2 corruptions.
4. [High] evidence-url coverage incomplete -> FIXED (signal sourceUrls + message-body urls vs the fetch allowlist) + corruptions.
5. [High] exposure goldens self-derived -> FIXED (frozen literal 9-Hormuz-id snapshot).
6. [Med] simulator circularity (Hormuz only) -> FIXED (literal pins for Red Sea + hurricane).
7. [High] injection exact-leak only -> IMPROVED (normalized scan resists whitespace/punct splitting; extended to playbooks + exposure rationales + action items) + corruptions; semantic paraphrase DISCLOSED -> G-5.
8. [Med] channels not real -> FIXED (every CSV payload through full `ingestSupplierCsv`; every article through the real `mapGdeltArticles` + `sanitizeText`).
9. [High] deliberate Sentinel->Atlas misclassification + closed `eventType` enum -> DISCLOSED as owner-gated: the Sentinel->Atlas handoff validation is the agent core (D, `GEMINI_API_KEY`); `threatCard.eventType` stays an OPEN string BY DOCUMENTED SCHEMA DESIGN (Phase 4 owns the closed vocab) — forcing an enum now contradicts it (refuted, with rationale). The off-taxonomy SECTOR grader is the pre-key portion.
10. [Med] playbook grounding -> FIXED (groundedClaimIds accept exposure id OR a resolvable sourcePath); playbook-step prose numeral grading DISCLOSED as deferred (playbooks ground via ids, not inline claims).

**Re-verify after the fixes — GREEN** (verify 293->294 passed/10 skipped; coverage above floor across all rounds).

## Gate review — confirm round

**acceptance-gate (fresh independent judge): gates 1 grill / 3 verify / 4 enterprise+taste / 5 anti-slop = PASS; gate 2 cross-model = hold-for-closure.** It judged the fixes "real and not papered over", verified the load-bearing claims against the schema (product-id fabrication now graded; `productRunouts` is the only product surface in the V2 packet), and confirmed the pre-key scope is honestly labelled (no overclaim that the live pipeline is verified). One real advisory: `exposureResults[].rationale` was a downstream-of-Sentinel prose surface silently excluded from the injection scan. **FIXED** (`720ff25`): rationales added to the scanned `draftedProse` + a corruption twin; `threatCard.summary` stays excluded (it IS Sentinel's own sanitized synopsis) with the exclusion justified in-code.

## Cross-model final stamp — DISCHARGED CLEAN (2026-06-18, APPROVED)

On 2026-06-17 the final stamp was usage-limit-blocked (shared Codex seat hit the cap, resets ~22:53) and the closure-resume emitted no verdict — so it was banked. **2026-06-18 (cap reset): the banked fresh `codex-guarded exec` ran and returned `VERDICT: REVISE` with 2 NEW [High] findings** — the FIRST cross-model look at the round-2/round-2-plus code (round-1 reviewed pre-fix code; the acceptance-gate confirm was same-family). Both real, both fixed (commit `2844085`):
1. **[High] `sourcePath` not restricted to structured inputs** — a claim could self-cite `supplierMessages[0].claims[0].value` (its own output) and satisfy both citation directions circularly. Fix: `gradeCitationCoverage` checks each claim's sourcePath ROOT against `CITATION_INPUT_ROOTS = {threatCard, publicSignals, exposureResults, simulation}` before resolving; Dispatcher/Strategist/meta roots fail. + corruption + unit test.
2. **[High] evidence-url grading not "every URL in any output"** — only threat-card/signal/message-body urls were scanned. Fix: `gradeEvidence` scans EVERY rendered prose surface (threat summary, signal summaries, exposure rationales, messages, playbook summary+steps, action-item titles, data gaps) vs the allowlist. + corruption.

**Re-verify after the fixes — GREEN** (verify 297 passed/10 skipped; coverage 82.4/82.2/80.4/72.1 above floor). **Codex closure (fresh exec, `2844085`): `VERDICT: APPROVED`** — both findings RESOLVED, no new defects. The mandatory cross-model gate is now DISCHARGED with a clean APPROVED stamp (not infra-banked). Lesson F-7 logged (a same-family confirm is not a cross-model substitute; the first true cross-model pass on changed code found 2 reals).

## Status: F COMPLETE + FULLY GATED. maker + verify(GREEN, 297) + coverage(82.4/82.2/80.4/72.1 above the ratcheted floor) + acceptance-gate(BLOCK->fix->confirm gates 1/3/4/5 PASS) + Codex cross-model(REVISE×10 round-1 -> addressed -> REVISE×2 final -> addressed -> **APPROVED** `2844085`). 7 lessons logged. Push HELD (owner gate).
